# 硬件实物核对清单（上电前必做）

> 版本：2026-09-14
> 适用对象：学长（张振民）毕设遗留的「灯哥开源四足机器狗」实物
> 目的：在开始 ESP-IDF C 迁移（P0）之前，把**代码假设**和**实物真相**逐项对齐。
> 纪律：**凡是本清单没测到的，一律视为未知**。论文和答辩 PPT 里的参数只作对照，不作事实依据。

> ### 📄 关于本文件的两种副本
>
> 本文件同时存在于两处：
>
> | 位置 | 角色 |
> |---|---|
> | 仓库内 `MechanicalDogUpgrade/硬件实物核对清单.md` | **权威版本**，进 git，随项目演进 |
> | 桌面 `硬件实物核对清单.md` | 现场**打印 / 手填**用副本 |
>
> **修改请改仓库版，然后覆盖桌面副本**，避免两边不一致。
> 桌面副本是为了在实验台旁边用笔勾选，不要单独编辑它。

---

## 0. 使用说明

### 0.1 判据来源

| 来源 | 可信度 | 说明 |
|---|---|---|
| `micropython/PA_SERVO.py`、`padog.py`、`config_s.py`、`mech_arm.py` | 高（真实在跑的代码） | 引脚、地址、通道、中位角从这里来 |
| `HANDOFF.md`、`ESP-IDF_C迁移表.md` | 高（上一轮实测记录） | 含实测 I2C 扫描结果 |
| `机械狗三维图\*.SLDPRT/SLDASM` 文件名 | 中（设计意图） | 只能证明**设计时选了什么**，不能证明现在装的是什么 |
| 论文 / 答辩 PPT | **低** | 已发现大量与代码不符的声称，仅用于对照 |

### 0.2 已确认的事实（不必重测，但可复核）

- 主控：ESP32 DevKit 类，USB-SERIAL **CH340 → COM4**，波特率 115200
- 主 I2C：**SDA=GPIO21 / SCL=GPIO22 / 100 kHz**
- PCA9685 两块：**0x40**（左前 0-2、左后 3-5）、**0x41**（右前 0-2、右后 3-5）
- 上次实测 I2C 扫描结果：`0x40`、`0x41`、`0x70`
- 状态灯：GPIO2
- ~~MPU6050：代码按 **0x68/0x69** 写，但**扫描未发现** ← 头号阻塞项~~
  ✅ **已结案：本机没有 IMU**（软件 4 次验证 + 2026-09-19 实物目视确认）
- 板上 `main.py` 曾被 uPyCraft 截断到 256 字节，已从备份恢复

### 0.2.0 ✅ 2026-09-19 实物目视确认（用户开壳核对）

这一节是**用户亲眼看到的**，可信度等同实测。

| 项目 | 确认结果 | 备注 |
|---|---|---|
| **IMU** | **没有 MPU6050 / GY-521** | 第 5 次独立确认，这下彻底结案 |
| **主控板** | 丝印 **`HW-394`**，`WiFi+BT` | 查证：HW-394 ≈ **ESP-WROOM-32 开发板**，与实测一致（普通 ESP32、rev 3.1、4MB Flash、CH340） |
| **降压模块 ×2** | 丝印 **`DC-DC` / `HW-674` / `W103`** | 查证：**HW-674 = XL4016E1 芯片的降压模块，8A / 200W 级** |
| **舵机驱动** | **两片 PCA9685** | 与地址 `0x40`/`0x41` 一致 |
| **电池** | **11.1 V / 3500 mAh** | 3S 锂电。论文写 2200mAh，实物是 3500 —— 以实物为准 |
| **上电顺序** | **插电池 + 打开开关后舵机才能驱动** | ⇒ **舵机母线 V+ 来自电池那一路**，与"PCA9685 逻辑电走 USB"并不矛盾（两路分开） |
| 控制方式 | 手机先连上 ESP32 才能遥控 | 即手机热点 + 狗做 STA，浏览器访问狗的 IP |

#### 由此得到的电源树（**待万用表复核**）

```
11.1V 3500mAh 3S 电池
   └─ 电源开关
        ├─ HW-674 (XL4016E1, 8A) ──► 5V ?  ──► 逻辑（ESP32 / PCA9685 VCC）
        └─ HW-674 (XL4016E1, 8A) ──► 6V ?  ──► PCA9685 V+ ──► 12+3 个舵机
USB Type-C ─────────────────────────► 逻辑 5V / 3.3V（实测：不插电池 PCA9685 逻辑照常应答，273 µs）
```

⚠️ **两个待复核点**（各量一下就知道）：

1. **两个 HW-674 的输出各是多少伏？** 预期一个 5V（逻辑）、一个 6V（舵机）。
   XL4016E1 是**可调**降压，输出电压由板上的电位器决定，必须实测。
2. **逻辑 5V 是否与 USB 的 5V 接在同一根线上？**
   如果是，则"插着 USB 又开电池"时两个电源在同一条轨上 —— 可能互相灌电流。
   这种情况下**插 USB 调试时建议先不开电池**（但那样舵机不动，见下方说明）。
   实测方法：万用表量 ESP32 板的 5V 引脚与 HW-674 输出是否导通。

#### 电流预算（比原先乐观得多）

| 项 | 数值 |
|---|---|
| 单个 HW-674 (XL4016E1) 额定 | **8 A** |
| 12 个腿部舵机额定电流 | 12 × 1.4 A = 16.8 A |
| 15 个舵机全堵转（极限） | 12×3.5 + 3×3.0 ≈ 51 A |
| 实际行走电流 | **远低于额定**（不是 12 个同时满载） |

⇒ 8A 单路给 12 个舵机**偏紧但可能够用**（取决于是否所有的都上了负载）。
原先按"3A UBEC"估风险，现在改成"8A，需实测"。**必须实测母线电压在动作时的跌落**，
若跌落 > 0.5V 就要考虑加电容或双路并联给舵机供电（正好有两块 HW-674）。

### 0.2.1 ⚠️ 参考电路图判读结果（2026-09-14 新增，**最高优先级**）

用户提供了一张原理图照片（`paste.png`，2559×1599）。识别结果：**这是「灯哥菠萝狗」官方教程的原理图**——
图标题为 `PART 2 菠萝狗的设计思路 — 电控`，即**参考设计**，**不能直接等同于本台实物的接线**。

图中可辨认的元件：

| 位号/名称 | 型号 | 作用 |
|---|---|---|
| U2 | **ESP32-DEVKIT-C**（38 pin） | 主控，板上丝印 `ESP32-DEVKIT-C` |
| — | **PCA9685** | 舵机驱动（图中**只出现 1 片**） |
| — | **MPU6050** | 六轴 IMU，引脚 1 VCC / 2 GND / 3 SCL / 4 SDA / 6 XCL / 7 AD0 / 8 INT |
| — | **AMS1117**（-3.3） | LDO：`IN = VCC-5V` → `OUT = VCC-3.3` |
| — | **XL4005** | 5A 降压 DC-DC：`IN+/IN-` → `OUT+/OUT-` = **VCC-5V** |
| — | **XH2.54** | 电池连接器，网络名 `BAT+` / `BAT-` |

**电源树（从图中读出）：**

```
电池 ──XH2.54──► BAT+/BAT- ──► XL4005 ──► VCC-5V
                                              ├──► AMS1117 ──► VCC-3.3 ──► ESP32 pin1 (VDD3)
                                              ├──► ESP32 EXT 5V (pin19)
                                              ├──► PCA9685  VCC
                                              └──► MPU6050  VCC
```

**I2C 网络（逐行 6× 放大 OCR 确认，引脚号/GPIO 名/网络名在同一行）：**

| ESP32 pin | GPIO | 网络名 | 用途 |
|---:|---|---|---|
| 22 | IO22 | **MPU-SCL** | MPU6050 时钟 |
| 25 | IO21 | **MPU-SDA** | MPU6050 数据 |
| 27 | IO19 | **PCA-SCL** | PCA9685 时钟 |
| 28 | IO18 | **PCA-SDA** | PCA9685 数据 |

即：**参考设计里 IMU 和舵机是两条独立 I2C 总线**，不是共用一条！

> ### 🔴 由此产生三个必须优先解决的矛盾
>
> **矛盾 1：代码把两对引脚用反了。**
> `PA_SERVO.py` 写的是：
> ```python
> try:
>   _i2c_servo = I2C(0, scl=Pin(22), sda=Pin(21), freq=100000)   # ← 按参考图，这是 IMU 总线
> except Exception:
>   _i2c_servo = I2C(0, scl=Pin(19), sda=Pin(18), freq=100000)  # ← 按参考图，这才是舵机总线
> ```
> 代码把 **21/22 当主舵机总线**、18/19 只当失败回退。而 `I2C(0, scl=..., sda=...)` 构造函数
> **即使没接任何器件也不会抛异常**，所以 `except` 回退**永远不会触发** → 代码始终用 21/22 打舵机。
>
> **矛盾 2：和上次实测记录冲突。**
> `HANDOFF.md` 记录「在 21/22 上扫到 `0x40`、`0x41`、`0x70`」。
> 若参考图成立（21/22 = IMU 总线），21/22 上就不该出现 PCA9685 地址，而应出现 `0x68`。
> ⇒ **说明实物接线与这张参考图不一致**（大概率是学长按代码把舵机焊到了 21/22）。
>
> **矛盾 3：全项目代码从来没有扫过 18/19 上的 IMU。**
> - `PA_SERVO.py`：主 21/22，回退 18/19（但只用于舵机）
> - `PA_STABLIZE.py`：先 21/22，失败回退 **32/33**
> - `PA_WALK.py`：先 `PA_SERVO._i2c_servo`(21/22)，失败回退 **32/33**
>
> **没有任何一处把 18/19 当作 IMU 总线去扫描。**
>
> ### ✅ 由此得到一个可能翻盘的结论
>
> 如果实物是「PCA9685 在 21/22 + MPU6050 在 18/19」（与参考图的引脚功能相反、但和实测记录自洽），
> 那么 **MPU6050 很可能是好的，只是从来没有被扫描过**。所谓"硬件阻塞项"可能根本不存在。
>
> **⇒ 阶段 D0 的第一件事就是扫 18/19。这一条比清单里其它任何一项都优先。**
>
> ---
>
> ### 🔴 实测结果（2026-09-14，已在真机执行）：上述假设【被推翻】
>
> 用 `i2c.scan()`（权威 ACK 判定）+ 全引脚**双极性**扫描后确认：
>
> | 总线 | 引脚 | 实测结果 |
> |---|---|---|
> | A | SCL22 / SDA21 | **0x40, 0x41, 0x70**（两片 PCA9685；MODE1=0x21，PRESCALE=122 → 50.2 Hz） |
> | B | SCL19 / SDA18 | **空**（开内部上拉仍为空） |
> | C | SCL32 / SDA33 | **空**（开内部上拉仍为空） |
> | 全引脚 | 9 个引脚的 72 个有序组合 | 只有 `SCL22/SDA21` 命中；**任何组合都没有 0x68/0x69** |
>
> **结论：IMU 不在 18/19，也不在 32/33，也不在任何其它引脚组合上。这台机器狗上没有可用的 IMU。**
>
> 原假设（"IMU 在 18/19，只是从没被扫过"）不成立。但这次排查是必要的 —— 不做就无法排除。
> **F1 决策**相应地改为按"确认无 IMU"处理。
>
> ⚠️ 同时确认：**实物接线与参考电路图不一致。** 参考图把 21/22 分给 IMU，
> 但实物上 21/22 是唯一的 I2C 总线，挂的是两片 PCA9685。
> ⇒ **这张参考图不能用于推断本台实物的引脚分配**，只能用于理解设计意图与电源拓扑。

另外两个由此衍生的疑点：

- **PCA9685 的 VCC 接 VCC-5V**，而 ESP32 的 I2C 电平是 3.3V。PCA9685 的输入高电平门限约为 `0.7 × VCC`，
  在 VCC=5V 时约 3.5V > 3.3V，**处于临界状态**，是 I2C 偶发失败/`ENODEV` 的经典诱因之一。
  需实测在 5V VCC 下通信是否稳定；如不稳，把 PCA9685 的 VCC 改接 VCC-3.3（V+ 仍走舵机母线）。
- **ESP32 pin1 (VDD3) 被外部 VCC-3.3 倒灌**。DevKitC 板载已有 3.3V 稳压器，
  同时经 pin1 灌 3.3V 并在 USB 供电时，属于双电源竞争，**调试时建议只保留一种供电方式**。

> ⚠️ 该图**未包含**：舵机母线（V+）供电、电源总开关、第二片 PCA9685（0x41）、12 路舵机接口、ESP32-CAM。
> 因此**舵机供电与第二片 PCA9685 的接线仍完全未知**，A/C 阶段照做。

### 0.3 安全前置（先读，再动手）

1. **不要**用 ESP32 的 3.3V 或 5V 针脚给舵机供电。
2. 舵机电源与逻辑电源**必须共地**，但供电能力分开评估。
3. **未确认充电电路前，不要把 Type-C 当锂电池充电口。**
4. 上电前先用万用表量电池、降压、舵机母线电压。
5. 任何舵机测试**先架空狗腿或上支架**，不要让舵机堵转超过 1 秒。
6. 拆装前先摸一下降压模块和舵机外壳温度，烫手就说明上次有问题。
7. 全程**边测边拍照**（见附录 D），后面写迁移文档要用。

### 0.4 建议的工具

万用表、可调直流电源（限流档，最好 0-20V/0-10A）、杜邦线、鳄鱼夹、
USB 电流表（测 ESP32 逻辑功耗）、红外测温枪（测舵机/降压温度）、标签纸 + 记号笔。

---

## 阶段 A：断电目视核对（不接任何电源）

- [ ] **A1** 拍照记录**整机俯视、侧视、底部、打开机身内部**各 1 张
- [ ] **A2** 找到 **ESP32 开发板**，记录板上**丝印型号**（如 `ESP32-WROOM-32` / `ESP32-DevKitC` / 其它）
      - 实测型号：`__________________`
      - 代码假设：ESP32 通用 DevKit，引脚 21/22
      - ⚠️ 若不是 WROOM-32 而是 **ESP32-S3**，GPIO21/22 的可用性要重新评估
- [ ] **A3** 记录 ESP32 板上**可用 GPIO 丝印**，确认是否真有 `21`、`22`、`2`、`12`、`18`、`19`、`32`、`33`
      - 缺失的丝印：`__________________`
- [ ] **A4** 数**舵机数量**：___ 个
      - 代码假设：**12 个腿部舵机**（4 腿 × 3 关节）+ **3 个机械臂舵机**（大臂/小臂/夹爪）= 15
- [ ] **A5** 抄下**每个舵机外壳上的型号丝印**（尤其是腿部 12 个）
      - 3D 图线索：腿部用 `servo_spt5435LV`，机械臂用 `MG995`
      - 实测腿部型号：`__________________`
      - 实测机械臂型号：`__________________`
      - ⚠️ **A5 是本次核对最关键的一项**，见 A6
- [ ] **A6** ⚠️ **确认舵机是 180° 版还是 270° 版**
      - 背景：SPT5435LV 官方同时存在 **-180 / -180W / -270** 版本；270° 版的脉冲映射是 **500–2500µs → 270°**
      - 而 `PA_SERVO.py` 写死 `Servos(..., min_us=500, max_us=2500, degrees=180)`
      - **若是 270° 版，则 `degrees=180` 错误，全部角度指令都会偏小约 1/3，IK 结果不能直接用**
      - 判定方法：① 看舵机标签/包装；② 手动把舵机转到两端极限，用手机量角器估总量程；③ 用舵机测试仪给 500µs / 2500µs，看是否是 270°
      - 结论：`□180°  □270°  □不确定`
- [ ] **A7** 找到两片 **PCA9685 板**，确认：
      - 型号/丝印：`__________________`
      - 地址跳线 **A0–A5** 的焊接状态（拍特写）：`__________________`
      - 板上**绿色接线端子**分别接了什么（拍特写）
      - VCC 是接 **3.3V 还是 5V**：`__________________`
      - V+ 端子接的是什么电源：`__________________`
      - ⚠️ PCA9685 的 V+ 是舵机电源输入，**绝不能**和 ESP32 的 5V 混接
- [ ] **A8** 找到 **MPU6050 模块**（或在机身内确认**找不到**）
      - 位置：`__________________`
      - 丝印：`__________________`
      - AD0 引脚接高/低/悬空：`__________________`
      - **如果找不到 → 记录「实物无 IMU」，直接跳到 F 节决策项**
- [ ] **A9** 找到**电源总开关**（3D 图有 `rocker_switch-1-solid1`）
      - 类型/额定电流丝印：`__________________`
      - 它切断的是**电池总正**还是**舵机母线**：`__________________`
- [ ] **A10** 找到**电池**，抄下标签
      - 标称电压：`______V`（论文写 11.1V，PPT 写 7.4V，**两处矛盾，以实物为准**）
      - 容量：`______mAh`
      - 放电倍率 C：`______C`
      - 接头类型：`__________________`（XT60 / T 插 / 其它）
      - 有几块电池：`______`
- [ ] **A11** 找到**降压/BEC 模块**，抄下型号与额定输出
      - 型号丝印：`__________________`（常见：LM2596 / MP1584 / SY8120B / TPS5450 / AMS1117）
      - 输入范围：`______`
      - 输出：`______V / ______A`
      - ⚠️ **额定电流必须能覆盖所有舵机的堵转电流之和**，见附录 A
- [ ] **A12** 检查是否有 **BMS / 保护板 / 充电模块**
      - 有/无：`______`
      - 丝印：`__________________`
- [ ] **A13** 检查舵机母线上是否有**大电容**（并在线性稳压前后）
      - 容量/耐压：`__________________`
- [ ] **A14** 检查是否有**电压/电流采样电路**（分流电阻、INA226、分压电阻）
      - 有/无：`______` 丝印/位置：`__________________`
      - 代码里**完全没有**这一项，若实物有则属新发现
- [ ] **A15** 确认 **Type-C / Micro-USB 口**接的是什么
      - 只接 ESP32：`□是 □否`
      - 是否连到电池/充电电路：`__________________`
- [ ] **A16** 辨认那块**蓝色 `HW-674` / `RAD` 板**的功能
      - 丝印全称：`__________________`
      - 它连接了哪些线：`__________________`
      - 判断结论：`__________________`
- [ ] **A17** 检查**接线是否有明显问题**：破皮、虚焊、杜邦线松动、冷压端子未压紧
      - 记录：`__________________`
- [ ] **A18** 检查 **3D 打印件**是否有裂纹/断裂（腿部支撑件是主要承力件）
      - 记录：`__________________`

### A19–A25（由参考电路图新增，见 §0.2.1）

- [ ] **A19** 确认主控是 **38-pin ESP32-DevKitC**（参考图 U2）
      - 排针总数：`____`（应为 38）  板上丝印：`__________________`
      - 是否与参考图的引脚编号一致？`□是 □否`
- [ ] **A20** 确认**电池连接器**类型
      - 是否 **XH2.54**（2.54mm 带扣）？`□是 □否`  实测：`__________________`
      - 是否为图中标注的 `BAT+ / BAT-` 两线？`□是 □否`
- [ ] **A21** 确认**降压模块**是 **XL4005**（5A DC-DC）
      - 丝印：`__________________`（参考图：XL4005，`IN+/IN-` → `OUT+/OUT-`）
      - 输出是否为 `VCC-5V`？`□是 □否`
- [ ] **A22** 确认 **LDO** 是 **AMS1117-3.3**
      - 丝印：`__________________`（参考图：AMS1117，`IN=VCC-5V` → `OUT=VCC-3.3`）
      - 是否有散热焊盘/覆铜？`□有 □无`
- [ ] **A23** ⚠️ **确认 PCA9685 的 `VCC`（逻辑电源）接 5V 还是 3.3V**
      - 参考图接的是 **VCC-5V** → 与 ESP32 的 3.3V 逻辑处于**临界电平**，可能是 `ENODEV` 诱因
      - 实测 VCC 引脚电压：`______V`
      - 若为 5V，是否观察到 I2C 通信不稳？`__________________`
      - 建议：改成 3.3V（`V+` 舵机电源不变），或加电平转换
- [ ] **A24** 确认 MPU6050 模块形态与 **AD0** 焊接状态
      - 是否 GY-521 / 蓝色小板？`□是 □否`  丝印：`__________________`
      - AD0 是否被焊到 GND（决定 0x68）或 VCC（0x69）？`__________________`
      - 模块 VCC 实测：`______V`（参考图给的是 VCC-5V；GY-521 板载 LDO 可接受 5V）
- [ ] **A25** ⚠️ **确认上电时是否存在"双电源"**
      - 参考图把 `VCC-3.3` 灌进 ESP32 pin1（VDD3），而 DevKitC 板载已有 3.3V 稳压器
      - 若同时插 USB 又接电池 → 两路 3.3V 竞争
      - 实测：插 USB 时是否也有电池供电？`□是 □否`
      - **调试期间只保留一种供电方式**

---

## 阶段 B：万用表静态核对（仍不接电源）

- [ ] **B1** 用**通断档**确认电池接头**不短路**（正负极之间阻值应为几百 Ω 以上或不导通）
      - 实测：`______Ω`
- [ ] **B2** 断开电池，量**舵机母线正负极之间**的电阻
      - 实测：`______Ω`（若接近 0Ω → 母线短路，**禁止上电**）
- [ ] **B3** 量 **PCA9685 V+ 与 GND** 之间电阻，确认无短路
      - 实测：`______Ω`
- [ ] **B4** 确认 **ESP32 GND 与舵机母线 GND 是通的**
      - `□通  □不通`（不通 → 必须补共地，否则 PWM 信号无效）
      - 实测：`______Ω`
- [ ] **B5** 用通断档**沿 I2C 线追线**，确认 PCA9685 的 SDA/SCL 最终连到 ESP32 的哪两个脚
      - 实测 SDA → GPIO `______`
      - 实测 SCL → GPIO `______`
      - 与代码假设（21/22）一致？`□是 □否`；若否 → **代码里的引脚必须改**
- [ ] **B6** 同样追线确认 **MPU6050 的 SDA/SCL** 接到哪里
      - 实测 SDA → GPIO `______`，SCL → GPIO `______`
      - 是共在 21/22 上，还是走 18/19 或 32/33？`__________________`
      - ⚠️ 代码里有三条可能路径：主 21/22、备用 18/19、IMU 备用 **32/33**（`PA_WALK.py`/`PA_STABLIZE.py`）
- [ ] **B7** 确认 MPU6050 的 **VCC** 接到 3.3V 还是 5V
      - 实测：`______V`（多数 GY-521 模块板载 LDO，可接 5V；裸芯片只能 3.3V）
- [ ] **B8** 量 I2C 线上**是否有上拉电阻**（PCA9685 板上通常有 10k）
      - 实测 SDA–VCC：`______kΩ`，SCL–VCC：`______kΩ`
      - ⚠️ 如果两块 PCA9685 **都**带 10k 上拉，并联后等效 5k，一般没问题；但若拉到不同电压轨会出问题
- [ ] **B9** 逐条**追出每个舵机的信号线**去了哪个 PCA9685 的哪个通道
      - 填附录 C 的「实测通道」列
      - ⚠️ 这一步最费时间但最值钱：它决定 C 版的 `servo_map.c` 对不对
- [ ] **B10** 确认**机械臂 3 个舵机**的信号线落点
      - 大臂 → `0x____ ch____`（代码假设 `0x40 ch6`）
      - 小臂 → `0x____ ch____`（代码假设 `0x40 ch7`）
      - 夹爪 → `0x____ ch____`（代码假设 `0x41 ch6`）
      - 是否有第 4 个舵机（论文声称 4 自由度 SCARA）？`□有 □无`
- [ ] **B11** 实测**机械结构关键尺寸**，与代码几何参数对照

| 参数 | 代码变量 | 代码值 | 实测值 | 一致? |
|---|---|---|---|---|
| 大腿连杆长 | `l1` | 130 mm | `______` | `□` |
| 小腿连杆长 | `l2` | 138 mm | `______` | `□` |
| 前后腿髋轴间距 | `l` | 230 mm | `______` | `□` |
| 左右腿间距 | `w` | 220 mm | `______` | `□` |
| 机身宽/相关尺寸 | `b` | 120 mm | `______` | `□` |
| 整机质量 | — | 论文 3.2 kg | `______` | `□` |

- [ ] **B12** 确认**状态灯**实际接在哪个 GPIO
      - 实测：GPIO `______`（代码假设 GPIO2，且 `0`=亮 / `1`=灭）

---

## 阶段 C：分步上电与电压测量

> 严格按 C1 → C7 顺序。每一步测完再进下一步，**不要一步到位全上电**。

- [ ] **C1** 只接 **ESP32 逻辑供电**（Type-C 或开发板 USB），**不接舵机母线**
      - 测 ESP32 3.3V 引脚：`______V`（应在 3.2–3.4V）
      - 测 ESP32 5V/VIN 引脚：`______V`
      - 用 USB 电流表读逻辑功耗：`______A`
      - 观察：板载电源灯是否稳定、有无发热
- [ ] **C2** 保持 C1 状态，**只接电池**（通过总开关），**仍然断开舵机母线**
      - 测电池输出：`______V`
      - 测降压模块输入：`______V`
      - 测降压模块输出（空载）：`______V`
      - 测降压模块空载温升（1 分钟后）：`______℃`
- [ ] **C3** 接入**舵机母线**（PCA9685 V+），**先不装机械负载 / 架空狗腿**
      - 测舵机母线电压（静态）：`______V`
      - 观察电压是否会掉（若静态就掉 → 电源或电容有问题）
- [ ] **C4** 逐个舵机**缓慢**给中位信号，**每次只动一个**
      - 记录：动到第 ___ 个时开始出现异常
      - 单舵机动作时母线电压：`______V`（掉压超过 0.5V 就要警惕）
      - 母线总电流（用钳形表/电流表）：`______A`
- [ ] **C5** 测**单舵机堵转电流**（用手轻挡输出轴，<1 秒，立刻放开）
      - 实测峰值：`______A`
      - 对比附录 A 的官方值 3.5A
      - ⚠️ **若明显超过官方值 → 舵机可能已损坏或型号不符**
- [ ] **C6** 全部 15 个舵机同时通电（保持中位，**不动作**）
      - 母线静态电流：`______A`
      - 母线静态电压：`______V`
      - 降压模块温度（3 分钟后）：`______℃`
      - 舵机外壳温度：`______℃`
- [ ] **C7** 记录**整机功耗与续航基线**
      - 站立待机总电流：`______A`
      - 按电池容量推算理论续航：`______min`
      - 论文声称 45 min，PPT 未给，自行核算

---

## 阶段 D：I2C 与设备识别（接 ESP32 上电，跑扫描）

> 这一步**不需要舵机母线供电**，只测 I2C 从设备是否存在。

### D0 —— 两对引脚判定 ✅ **已完成（2026-09-14）**

> 起因见 §0.2.1。**已执行，完整结论见 §0.2.1 的实测结果表。**

- [x] **D0.1** 扫描三条总线 —— ✅ 已完成（用 `diagnostics/board_scan_i2c.py`）
- [x] **D0.2** 记录三组结果 —— ✅ 已完成

| 总线 | 引脚 | 实测扫描结果 | 判读 |
|---|---|---|---|
| A | SCL22 / SDA21 | `0x40, 0x41, 0x70` | **两片 PCA9685 在这里**，是唯一的真实 I2C 总线 |
| B | SCL19 / SDA18 | `(空)` | 无任何器件 |
| C | SCL32 / SDA33 | `(空)` | 无任何器件 |

- [x] **D0.3** 最终结论 —— ✅ 已完成

| 问题 | 结论 |
|---|---|
| PCA9685 实际在哪对引脚？ | **21/22**（SCL=22, SDA=21）—— 与代码一致 |
| MPU6050 实际在哪对引脚？ | **不存在**：3 条总线 + 72 个引脚有序组合全部无 `0x68/0x69` |
| 与参考图一致吗？ | **不一致**：参考图说 21/22 是 IMU，实为 PCA9685 |
| 代码需要改吗？ | **引脚不用改**（舵机确实在 21/22）。IMU 相关代码在硬件到位前无法启用 |

- [x] **D0.4** IMU 数据验证 —— ⛔ **无法执行：IMU 不存在**

> **勘误**：`PA_IMU.py` 写 `0x1C = 0x08` → AFS_SEL=2 → **±4g，8192 LSB/g**，
> 所以静止时 `AcZ` 应约 **±8192**（不是本文档早期写的 ±16384）。
> `0x1B = 0x08` → FS_SEL=1 → ±500 dps，65.5 LSB/(deg/s)，与 `PA_IMU.py` 里 `/65.5` 一致。

### D1–D5 —— 完整扫描与排查

> D1/D2 已由 D0 与 `diagnostics/board_scan_all_pins.py` 覆盖完成。
> D3 表中的 `0x68/0x69` 两行结论为「未出现」。**D4 的排查流程保留**，
> 用于将来加装 IMU 后仍读不到时使用。D5 未做（PCA9685 已在正常通信，PRESCALE=122 已证明）。

- [ ] **D1** 以 D0 确定的**正确总线**为准，重跑一次完整扫描
- [ ] **D2** 记录扫描结果
      - 实测：`__________________`
      - 预期：舵机总线上**至少** `0x40`、`0x41`；IMU 总线上**至少** `0x68`（或 `0x69`）
      - 上次记录（仅在 21/22 上扫）：`0x40`、`0x41`、`0x70`
- [ ] **D3** 逐项判定扫描结果

| 地址 | 应是什么 | 本次是否出现 | 判读 |
|---|---|---|---|
| `0x40` | PCA9685 左半身 | `□有 □无` | 无 → 跳线/供电/接线问题 |
| `0x41` | PCA9685 右半身 | `□有 □无` | 无 → 同上 |
| `0x68` | MPU6050（AD0=低） | `□有 □无` | **有 → IMU 存在，重大好消息** |
| `0x69` | MPU6050（AD0=高） | `□有 □无` | 有 → 改 `PA_IMU.py` 默认地址 |
| `0x70` | PCA9685 all-call 广播 | `□有 □无` | 属正常，**别当成 IMU** |
| 其它 | `______` | `□有 □无` | 记录并判断 |

- [ ] **D4** 若 D0 三个阶段都**没有**扫到 `0x68/0x69`，按顺序排查（**每步都记录**）：
  1. **先在 18/19 上扫**（参考图的 IMU 总线，项目里从未扫过）：`__________________`
  2. **再在 32/33 上扫**（代码的 IMU 回退路径）：`__________________`
  3. 模块是否真的焊在总线上（B6 追线结果）
  4. 模块供电电压实测：`______V`（参考图给的是 VCC-5V）
  5. AD0 电平实测：`______V` → 对应地址 `0x68`/`0x69`
  6. 有没有和 PCA9685 抢总线（断开 PCA9685 后重扫）：`__________________`
  7. 用**另一个已知好的** MPU6050/GY-521 替换试一次：`__________________`
  8. 结论：`□模块损坏  □未接线  □供电问题  □地址冲突  □实物根本没有IMU`
- [ ] **D5** 确认 **PCA9685 频率**可正常设置（代码设 `freq=50`）
      - 读回 prescale 值：`______`（50Hz 对应约 121）
      - ⚠️ 若读回异常 → I2C 写时序或供电有问题

---

## 阶段 E：单通道舵机安全测试

> 只在 D 阶段确认 `0x40/0x41` 正常后才做。**一次只动一个通道，先拆下机械臂/架空狗腿。**

- [ ] **E1** 对 `0x40 ch0`（代码称「左前髋」）发中位，确认**实际动的是哪个关节**
      - 实测对应关节：`__________________`
      - 与代码一致？`□是 □否`
- [ ] **E2** 逐个测 12 个腿部通道，填附录 C 的「实测对应关节」列
      - ⚠️ 重点验证代码的**逻辑腿编号 vs 物理位置**是否一致：

| 代码逻辑腿 | 代码含义 | 使用的通道 | 实测物理位置 | 一致? |
|---|---|---|---|---|
| 腿1 `init_1*` | 左前 | 0x40 ch0/1/2 | `__________` | `□` |
| 腿2 `init_2*` | 右前 | 0x41 ch0/1/2 | `__________` | `□` |
| 腿3 `init_3*` | 右后 | 0x41 ch3/4/5 | `__________` | `□` |
| 腿4 `init_4*` | 左后 | 0x40 ch3/4/5 | `__________` | `□` |

- [ ] **E3** 验证每个关节的**转动方向**是否符合代码预期
      - 代码中髋关节：`init_*p + h`（同向）
      - 代码中小腿：左前/左后用 `init_1s-90+...`，右前/右后用 `init_2s+90-...`（**左右符号相反**）
      - 实测方向不一致的通道：`__________________`
- [ ] **E4** 验证**角度限幅**是否够安全
      - 代码 `_clamp_deg()` 统一限 0–180
      - 实测有机械干涉的角度区间：`__________________`
      - ⚠️ 记录会撞机械限位的通道，C 版要加**每通道独立限幅**
- [ ] **E5** 测机械臂 3 个舵机（大臂/小臂/夹爪）
      - 大臂范围实测：`______°`（代码 `arm_upper_min/max = 0/180`，中位 145）
      - 小臂范围实测：`______°`（代码 `arm_fore_min/max = 30/140`，中位 125）
      - 夹爪开合实测：`______°`（代码 `arm_grip_open/close = 90/180`）
      - 是否有第 4 轴：`□有 □无`
- [ ] **E6** 确认 `config_s.py` 里的**中位角**是否还适用于当前机械装配
      - 记录当前 12 个中位值（若与代码一致则勾选即可）
      - 若装配已改动 → 后面必须重新标定

| 变量 | 代码值 | 实测中位 | 备注 |
|---|---:|---:|---|
| `init_1p` 左前髋 | 102 | `____` | |
| `init_1h` 左前大 | 84 | `____` | |
| `init_1s` 左前小 | 92 | `____` | |
| `init_2p` 右前髋 | 96 | `____` | |
| `init_2h` 右前大 | 91 | `____` | |
| `init_2s` 右前小 | 85 | `____` | |
| `init_3p` 右后髋 | 108 | `____` | |
| `init_3h` 右后大 | 78 | `____` | |
| `init_3s` 右后小 | 68 | `____` | |
| `init_4p` 左后髋 | 92 | `____` | |
| `init_4h` 左后大 | 98 | `____` | |
| `init_4s` 左后小 | 102 | `____` | |

---

## 阶段 F：待决策项（核对结果决定后续架构）

> 这些是**必须在写 C 代码之前拍板**的。不要带着未决项进入 P1。

| 编号 | 决策点 | 触发条件 | 选项 | 你的决定 |
|---|---|---|---|---|
| **F1** | IMU 怎么办 | **已确认本机无 IMU**（2026-09-14 实测，见 §0.2.1） | A. 暂不加装：先完成 P0–P3（开环步态 + Web），P4 再补 IMU<br>B. 立即加装 GY-521（MPU6050）挂到唯一总线 21/22<br>C. 装更好的 ICM-42688 / BMI270<br>D. 永不加装，放弃姿态闭环 | `______` |
| **F2** | 舵机是 180° 还是 270° | A6 结论 | A. 180° → 沿用 `degrees=180`<br>B. 270° → C 版必须改脉冲↔角度映射，并**全部重新标定** | `______` |
| **F3** | 舵机母线供电能力够不够 | C5/C6 结果 vs 附录 A 预算 | A. 够 → 沿用<br>B. 不够 → 换更大 BEC / 加电容 / 分组供电 | `______` |
| **F4** | 是否新增电压/电流检测 | A14 结论 | A. 已有 → 接入<br>B. 没有 → 按新增硬件设计（不凭空指定引脚） | `______` |
| **F5** | 电源拓扑是否记入文档 | A9–A13 汇总 | 必须画一张**手绘电源树**并拍照归档 | `______` |
| **F6** | 机械臂真实自由度 | B10/E5 结论 | A. 3 自由度（大臂/小臂/夹爪）→ 与 `mech_arm.py` 一致<br>B. 4 自由度 → C 版需新增一轴 | `______` |
| **F7** | 是否继续用现有 ESP32 | A2/B5 结论 | A. 引脚与代码一致 → 继续用<br>B. 不一致 → 改代码引脚表 | `______` |
| **F8** | ESP32-CAM 是否要修 | 已知损坏 | A. 不修，视觉后置（推荐）<br>B. 换新模块 | `______` |
| **F9** | 是否确认充电电路 | A15 结论 | A. 确认 → 可当充电口<br>B. 未确认 → **继续禁止当充电口** | `______` |

---

## 附录 A：舵机参数（已查证）

### A.1 腿部舵机 — SPT5435LV（来源：SPT 官方代理站）

| 项目 | 4.8 V | 6.0 V |
|---|---|---|
| 堵转扭矩 | 29 kg·cm | **35 kg·cm** |
| 动作速度 | 0.16 s/60° | 0.14 s/60° |
| 额定电流 | — | **1.4 A** |
| **堵转电流** | — | **3.5 A** |
| 静态电流 | — | 100 mA |

其它：全金属齿、半铝壳、双轴承、Futaba **25T** 输出轴、70 g、40.5×20×40.5 mm、
周期 20 ms、信号频率 330 Hz、中位 1500 µs、线序 **棕-/红+/橙S**。
**存在 -180 / -180W / -270 多个版本，脉冲-角度映射不同。**

> ⚠️ 论文/PPT 声称「15 kg·cm」，与实物可能的 29–35 kg·cm 差一倍以上。以实物为准。

### A.2 机械臂舵机 — MG995（来源：公开资料汇总）

| 状态 | 电流 |
|---|---|
| 空载/待机 | 0.3–0.6 A |
| 额定负载 | 1.0–2.0 A |
| **堵转** | **2.5–3.2 A** |

工作电压 4.8–7.2 V，25T 花键，扭矩约 10–13 kg·cm。

### A.3 母线电流预算（**用来选 BEC**）

| 场景 | 计算 | 结果 |
|---|---|---|
| 12 腿舵机额定 | 12 × 1.4 A | **16.8 A** |
| 12 腿舵机全堵转 | 12 × 3.5 A | **42 A** ⚠️ |
| 15 舵机全堵转（含臂） | 12×3.5 + 3×3.0 | **51 A** ⚠️ |
| 经验估算（按最恶劣 × 1.2） | | **≥ 20 A 持续** |

**结论**：常见的「6V 3A UBEC」**远远不够**。即使按额定值也需 16.8A。
必须实测母线电压在动作时的跌落；若跌落 > 0.5V，就会出现舵机抖动、ESP32 复位、
甚至 PCA9685 报 ENODEV 的现象（代码注释里正好记录过 ENODEV 问题）。

> 建议：母线并联 **≥1000 µF 低 ESR 电解电容**，主供电线 **≥16 AWG**。

---

## 附录 B：代码侧已知映射（用来和实物比对）

### B.1 I2C 与引脚

| 功能 | 代码位置 | 代码假设 |
|---|---|---|
| 主 I2C | `PA_SERVO.py` | `I2C(0, scl=22, sda=21, freq=100000)` |
| 主 I2C 备用 | `PA_SERVO.py` | `I2C(0, scl=19, sda=18)` |
| IMU 备用 | `PA_WALK.py` / `PA_STABLIZE.py` | `I2C(1, scl=32, sda=33)` |
| 状态灯 | `padog.py` | `Pin(2, OUT)` |
| 夹爪备用 | `mech_arm.py` | GPIO12，50 Hz（当前未启用） |
| 语音模块 | `example/workSpace/uart_control.py` | `UART(2, tx=17, rx=16, 115200)` |
| ESP32-CAM↔狗 | `esp32cam_v5.ino` | `Serial2` RX=3 / TX=1，115200 |

### B.2 逻辑通道 → 板卡/板载通道

| 逻辑通道 | 板 | 板载 ch | 代码含义 |
|---:|---|---:|---|
| 0 | 0x40 | 0 | 左前髋 |
| 1 | 0x40 | 1 | 左前大腿 |
| 2 | 0x40 | 2 | 左前小腿 |
| 3 | 0x40 | 3 | 左后髋 |
| 4 | 0x40 | 4 | 左后大腿 |
| 5 | 0x40 | 5 | 左后小腿 |
| 6 | 0x41 | 0 | 右前髋 |
| 7 | 0x41 | 1 | 右前大腿 |
| 8 | 0x41 | 2 | 右前小腿 |
| 9 | 0x41 | 3 | 右后髋 |
| 10 | 0x41 | 4 | 右后大腿 |
| 11 | 0x41 | 5 | 右后小腿 |
| — | 0x40 | 6 | 机械臂大臂 `arm_upper_ch=6` |
| — | 0x40 | 7 | 机械臂小臂 `arm_fore_ch=7` |
| — | 0x41 | 6 | 夹爪 `arm_grip_ch=6` |

> ⚠️ `PA_SERVO.py` 顶部注释把 `0x40 ch6` 叫做「底座」，但运行配置当「大臂」用。
> 源码注释与配置**不一致**，必须实物确认。

### B.3 代码引用的几何/步态参数（`config_s.py`）

`l1=130`、`l2=138`、`l=230`、`b=120`、`w=220`、`h=65`、`speed=0.065`、
`Kp_H=0.06`、`Kp_G=0.03`、`CG_X=0`、`CG_Y=28`、`walk_h=63`、`H_goal=81`、
`leg_len_ref=149`、`joy_fwd_sign=-1`、`trot_cg_f=0.52`、`trot_cg_b=0.85`、
`faai=0.42`、`hip_k_roll=0.06`、`hip_k_pitch=0.02`、`hip_k_turn=0.75`、`hip_delta_max=18.0`

### B.4 3D 图暴露的器件线索

| 文件 | 指向 |
|---|---|
| `servo_spt5435LV-1/-2-solid*.SLDPRT` | 腿部舵机 = SPT5435LV |
| `ARM MG995.SLDPRT`、`[Komp] Star Horn for MG995.SLDPRT` | 机械臂 = MG995（25T 舵盘） |
| `rocker_switch-1-solid1.SLDPRT` | 电源总开关 |
| `screw_4_0.5inch_90610A110-1..33` | 4 号自攻钉 ×33 |
| `screw_m3_12mm_90116A157-1..16` | M3×12 螺钉 ×16 |
| `servo_holder_left_inner` / `right_inner` | 舵机内侧固定件 |
| `髋关节.SLDASM`、`小腿.SLDASM`、`左腿.SLDASM`、`右腿.SLDASM` | 腿部装配 |
| `狗.SLDASM`、`quadruped.SLDPRT`、`quadruped.SLDDRW` | 整机装配与工程图 |
| `机仿生械臂.SLDASM`、`shou.SLDASM` | 机械臂与手爪 |
| `程序\esp32cam.SLDPRT` | 摄像头模块外框 |

> 注：`机械狗三维图\` 里**没有**电池、降压模块、PCA9685、ESP32 的 3D 模型，
> 说明这些是按通用件选用、未建模 → **必须实物确认**。

---

## 附录 C：通道实测记录表（建议打成 A4 手填）

```
PCA9685 0x40                               PCA9685 0x41
┌──────┬──────────┬──────────┬─────────┐  ┌──────┬──────────┬──────────┬─────────┐
│ 板载 │ 代码认为 │ 实测对应 │ 方向是否│  │ 板载 │ 代码认为 │ 实测对应 │ 方向是否│
│ ch   │ 的关节   │ 的关节   │ 一致    │  │ ch   │ 的关节   │ 的关节   │ 一致    │
├──────┼──────────┼──────────┼─────────┤  ├──────┼──────────┼──────────┼─────────┤
│  0   │ 左前髋   │          │         │  │  0   │ 右前髋   │          │         │
│  1   │ 左前大腿 │          │         │  │  1   │ 右前大腿 │          │         │
│  2   │ 左前小腿 │          │         │  │  2   │ 右前小腿 │          │         │
│  3   │ 左后髋   │          │         │  │  3   │ 右后髋   │          │         │
│  4   │ 左后大腿 │          │         │  │  4   │ 右后大腿 │          │         │
│  5   │ 左后小腿 │          │         │  │  5   │ 右后小腿 │          │         │
│  6   │ 大臂(?)  │          │         │  │  6   │ 夹爪     │          │         │
│  7   │ 小臂     │          │         │  │  7   │ (未用?)  │          │         │
└──────┴──────────┴──────────┴─────────┘  └──────┴──────────┴──────────┴─────────┘
```

---

## 附录 D：必须拍照/存档的清单

- [ ] D1 整机四视图（俯/侧/底/正面）
- [ ] D2 打开机身后的**内部全景**
- [ ] D3 ESP32 板**正反面丝印特写**
- [ ] D4 两块 PCA9685 的**地址跳线 + 端子接线**特写
- [ ] D5 每个舵机的**型号标签**特写
- [ ] D6 电池标签特写
- [ ] D7 降压模块丝印特写
- [ ] D8 BMS / 充电模块特写
- [ ] D9 蓝色 `HW-674`/`RAD` 板正反面特写
- [ ] D10 **手绘电源树**（电池→开关→降压→母线→各负载）
- [ ] D11 **手绘接线图**（ESP32 ↔ PCA9685 ↔ 舵机，标注 GPIO 与通道号）
- [ ] D12 机械臂装配特写（判断真实自由度）

---

## 附录 E：危险接线的后果对照（用来快速定位故障）

| 现象 | 最可能原因 | 对应检查项 |
|---|---|---|
| I2C 扫描无 `0x40/0x41` | SDA/SCL 接反、无共地、VCC 未接、跳线错 | B4、B5、A7 |
| 运行中偶发 `ENODEV` | I2C 线过长 / 上拉不足 / 电源跌落复位 | C4、C6、B8 |
| 扫到 `0x70` 却没 IMU | 把 all-call 地址误当 IMU | D3 |
| 扫不到 `0x68/0x69` | IMU 未装 / 供电 / AD0 / 总线冲突 | D4 全流程 |
| 舵机抖动、ESP32 复位 | 舵机母线与逻辑电共用 / BEC 电流不足 | A11、C6、附录 A.3 |
| 舵机发热、嗡嗡响 | 堵转 / 中位角错 / 机械干涉 | E4、E6、C5 |
| 角度整体偏小约 1/3 | **270° 舵机配了 180° 映射** | **A6** |
| 一上电就跳闸/发烫 | 母线短路 | B2、B3（**禁止上电前必做**） |
| 左右腿动作镜像错误 | 左右通道方向符号 | E3、B9 |

---

## 附录 F：参考电路图判读记录

### F.1 判读对象与方法

| 项目 | 说明 |
|---|---|
| 文件 | `C:\Users\boyi\AppData\Local\Temp\modlens-dsh-paste\p-Acr88E\paste.png` |
| 尺寸 | 2559 × 1599，8-bit RGB PNG |
| 来源判断 | 图标题 `PART 2 菠萝狗的设计思路 — 电控`，属**灯哥菠萝狗教程课件** |
| 方法 | 环境无视觉模型，改用 **Windows 内置 OCR 引擎**（`Windows.Media.Ocr`，zh-Hans-CN）<br>先用 2× 放大 + 2×2 重叠分块扫全文，再对关键区域做 **4×/6× 定向放大 + 逐行 word 级坐标**识别 |
| 可靠性 | 元件型号、网络名、引脚号 **高**（同一行内多次一致复现）；<br>导线走向、未标网络名的连接 **低**（图是照片，细线不可靠） |

### F.2 判读局限（必须知道）

1. 这是一张**屏幕照片**，不是矢量原理图，细线/交叉点无法可靠判读。
2. OCR **不能**判定"哪根线连到哪根线"，只能读出**文字**。
   ⇒ 所有"连接关系"结论都基于**网络名相同即相连**这一前提。
3. 图中**只出现 1 片 PCA9685**，而实物有 **2 片**（0x40、0x41）→ 图不完整或是参考设计。
4. 图中**没有**：舵机母线 V+、电源总开关、12 路舵机接口、ESP32-CAM、机械臂。
5. 因此本图**只能**用来：(a) 交叉验证引脚分配；(b) 理解电源拓扑意图。
   **不能**替代阶段 B 的实物追线。

### F.3 完整读出内容（去噪后）

**标题区**
- `PART 2 菠萝狗的设计思路`
- `电控`
- `U2` `ESP32-DEVKIT-C`

**ESP32-DEVKIT-C 左列（pin 1–19）**
`1 VDD3` / `2 EN` / `3 SENSOR_VP` / `4 SENSOR_VN` / `5 IO34` / `6 IO35` / `7 IO32` /
`8 IO33` / `9 IO25` / `10 IO26` / `11 IO27` / `12 IO14` / `13 IO12` / `14 GND` /
`15 IO13` / `16 SD2` / `17 SD3` / `18 CMD` / `19 EXT 5V`
- 网络：pin1 ← `VCC-3.3`；pin19 ← `VCC-5V`

**ESP32-DEVKIT-C 右列（pin 20–38）**
`20 GND` / `21 IO23` / `22 IO22` / `23 TXD0` / `24 RXD0` / `25 IO21` / `26 GND` /
`27 IO19` / `28 IO18` / `29 IO5` / `30 IO17` / `31 IO16` / `32 IO4` / `33 IO0` /
`34 IO2` / `35 IO15` / `36 SD1` / `37 SD0` / `38 CLK`
- 网络：`pin22 → MPU-SCL`、`pin25 → MPU-SDA`、`pin27 → PCA-SCL`、`pin28 → PCA-SDA`

**PCA9685 模块**
- 引脚：`VCC`(← `VCC-5V`) / `GND` / `SCL`(← `PCA-SCL`) / `SDA`(← `PCA-SDA`) / `OE`
- 图中出现引脚编号 `6`、`4`、`3`、`0`、`1`

**MPU6050 模块**
- 引脚：`1 VCC`(← `VCC-5V`) / `2 GND` / `3 SCL`(← `MPU-SCL`) / `4 SDA`(← `MPU-SDA`) /
  `6 XCL` / `7 AD0` / `8 INT`

**电源部分**
- `XH2.54`（电池连接器）
- `BAT+` / `BAT-`
- `XL4005`：`IN+` / `IN-` / `OUT+` / `OUT-`，输出网络 `VCC-5V`，`GND`
- `AMS1117`：`IN`(← `VCC-5V`) / `OUT`(→ `VCC-3.3`) / `GND`

### F.4 由本图派生的新增核对项

已并入正文：**A19–A25**（型号确认、PCA9685 VCC 电平、双电源）、**B13–B14**（见下）、
**D0**（两对 I2C 引脚判定，最高优先级）、**F1**（IMU 决策）。

- [ ] **B13** 用**通断档**分别确认 `GPIO21/22` 与 `GPIO18/19` 各自通向**哪个模块**
      - 21/22 → `□PCA9685  □MPU6050  □两者  □都没接`
      - 18/19 → `□PCA9685  □MPU6050  □两者  □都没接`
      - 与参考图（21/22=IMU、18/19=舵机）一致？`□一致 □相反 □不同`
- [ ] **B14** 确认 `VCC-5V` 与 `VCC-3.3` 两个电源轨的实际归属
      - `VCC-5V` 供给了：`□ESP32 EXT5V □PCA9685 VCC □MPU6050 VCC □其它 ______`
      - `VCC-3.3` 供给了：`□ESP32 pin1 □其它 ______`
      - PCA9685 的 `V+`（舵机母线）来自：`__________________`

---

## 核对完成判定

全部通过的标准：

- [ ] **D0 三组 I2C 扫描全部做完**，`GPIO21/22` 与 `GPIO18/19` 各通向哪个模块**有确定结论**
- [ ] 附录 D 的 12 张照片 + 2 张手绘图已归档
- [ ] 阶段 A 全部勾选，型号/数量/电压全部有实测值
- [ ] 阶段 B 的**追线结果**填满（GPIO 与通道号无空缺）
- [ ] I2C 扫描能看到 `0x40`、`0x41`；IMU 的存在性有明确结论（出现或确认不存在）
- [ ] 12 个腿部通道 + 3 个机械臂通道**逐个**动过且记录了物理位置
- [ ] F1–F9 九个决策点**全部拍板**
- [ ] 附录 A.3 的母线电流预算与实测值对得上

**只有全部完成后，才进入 ESP-IDF C 迁移的 P0（建工程 + 日志 + I2C 扫描 + PCA9685 单通道驱动）。**

---

## 附：本清单未覆盖、但已知的软件侧遗留问题（不属硬件核对，先记录备查）

1. `web_c.py` 用 `exec(req_data.replace('&',';'))` **把 URL 查询串当 Python 代码执行** —— C 版必须改成白名单字段 + 范围校验。
2. 标定保存（`key=sc`）会**直接重写 `config_s.py` 源码** —— C 版改为 NVS。
3. `PA_STABLIZE.stab()` 在主循环里被注释掉，IMU 闭环**从未生效**。
4. `PA_IMU.IMUupdate()` 是四元数互补滤波，**论文说的「卡尔曼滤波」不成立**。
5. 步态是硬编码 TROT/WALK 轨迹，**论文说的「改进型 CPG」不成立**。
6. Web 是阻塞式 HTTP GET 轮询（control.html ~120ms / drive.html 狗~80ms、臂~35ms），无 WebSocket / 无序号 / 无心跳。

---

## 附录 G：2026-09-14 实测记录与新增发现

### G.1 实测环境

| 项目 | 值 |
|---|---|
| 端口 | **COM4**（USB-SERIAL CH340，VID:PID=1A2C? 实测 `1A86:7523`） |
| 固件 | **MicroPython 1.13.0**（编译时间 2020-09-02），底层 **ESP-IDF v3.3.2** |
| 芯片 | ESP32（无 PSRAM 迹象，空闲堆约 95 KB） |
| Flash | 4 194 304 字节（4 MB） |
| MAC | `04:b2:47:83:cf:88` |
| 工具 | `mpremote 1.29.0` + `pyserial 3.5`（本轮新装到 Python 3.12） |

### G.2 I2C 扫描原始结论

```
自检  SCL=22 SDA=21 -> 0x40, 0x41, 0x70        （通过，证明探测方法有效）
总线A SCL=22 SDA=21 -> 0x40, 0x41, 0x70        MODE1=0x21  PRESCALE=122 -> 50.2 Hz
总线B SCL=19 SDA=18 -> (空)                    （开内部上拉仍为空）
总线C SCL=32 SDA=33 -> (空)                    （开内部上拉仍为空）
全引脚 9 引脚 / 72 个有序组合 -> 仅 SCL22/SDA21 命中，无任何 0x68/0x69
```

**PCA9685 判读**：`MODE1=0x21` = ALLCALL(bit0)+AI(bit5) 置位、SLEEP 已清；
`PRESCALE=122` → `25MHz/4096/(122-0.5) ≈ 50.2 Hz`。与 `PA_SERVO.py` 的 `freq=50` 完全吻合，
说明**舵机驱动链路是通的、代码在正常跑**。

### G.3 新增发现的软件问题（本轮实机暴露）

| 编号 | 问题 | 证据 | 对 C 迁移的影响 |
|---|---|---|---|
| **N1** | `padog.do_connect_STA()` 是 `while not wifi.isconnected(): pass`，**无超时、无重试上限** | 复位日志停在 `connecting to network...` 后卡死 | C 版必须有超时 + 失败降级（AP 模式或停车待命），否则同样开机即挂 |
| **N2** | MicroPython **1.13 的 `I2C` 对象没有 `deinit()`** | `AttributeError: 'I2C' object has no attribute 'deinit'` | 仅影响脚本；C 版用 `i2c_driver_delete()` 正常 |
| **N3** | `i2c.readfrom_mem()` 在总线无器件时**可能不抛异常而返回残留数据** | 出现 `SCL=5/SDA=21`、`SCL=32/SDA=21` 这类假阳性命中（SCL 根本未接线） | 探测器件要用 `scan()` 或严格校验 WHO_AM_I；**不要靠"读成功"判断存在** |
| **N4** | 固件为 2020 年的 **MicroPython 1.13**，非当前版本 | `os.uname()` | 若继续用 MicroPython 做对照测试，注意 API 差异（无 `deinit` 等） |

### G.4 因此更新后的结论

1. **硬件层面**：本机只有一个 I2C 总线（SCL22/SDA21），上面是两片 PCA9685（0x40、0x41）。
   **没有 IMU。** `HANDOFF.md` 里"MPU6050 为什么扫不到"这一疑问，答案就是**实物上没有**。
2. **参考电路图**：只能用于理解电源拓扑与设计意图，**不能用于推断本机引脚分配**。
3. **C 迁移的阻塞项更新**：
   - ~~IMU 硬件阻塞~~ → 改为 **待决策：是否加装 IMU**（F1）
   - **P0 不再被 IMU 阻塞**，可以立即开始：建 PlatformIO + ESP-IDF 工程、日志、I2C 扫描、PCA9685 单通道驱动
   - **P4（IMU 闭环）**在加装 IMU 之前无法进行
4. **可以立刻在 C 工程里复现验证的既有事实**：`0x40`/`0x41` 必现；12 路逻辑通道映射；
   `PRESCALE=122 @ 50.2Hz`。

### G.5 本轮新增的工具脚本（`MechanicalDogUpgrade/diagnostics/`）

| 文件 | 用途 |
|---|---|
| `board_scan_i2c.py` | 扫三条候选总线（21/22、18/19、32/33），带无上拉/内部上拉两种模式 |
| `board_scan_all_pins.py` | 9 个引脚 × 72 个有序组合的全引脚权威扫描 |
| `board_find_i2c.py` | 早期版本（v4），保留作参考，**已证明 `readfrom_mem` 探测有假阳性** |
| `hard_reset.py` | 用 pyserial 拉 DTR/RTS 硬复位（`mpremote` 进不去 raw REPL 时用） |

运行方式：

```powershell
cd C:\Users\boyi\Desktop\MechanicalDogUpgrade\diagnostics
python -m mpremote connect COM4 run board_scan_all_pins.py
```

> ⚠️ 经验教训：用 PowerShell 管道接 `Out-String` 会**缓冲全部输出**，
> 长时间运行被超时杀掉时一个字都拿不到。跑长脚本要直接输出或过滤后用 `Select-String -Context`。
