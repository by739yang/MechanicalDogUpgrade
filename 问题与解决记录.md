# 问题与解决记录（成长手册）

> 建立：2026-09-19
> 用途：记录本项目遇到的**每一个真实问题** —— 现象、根因、诊断路径、修法、教训。
> 它同时服务两个目的：
> 1. **技术资料**：下一个对话 / 下一位协作者遇到同类现象能直接查到答案。
> 2. **成长手册**：这是我们一起踩过的坑。包括我猜错的判断 —— 那些部分不删。
>
> 约定：
> - 只写**已经真实发生并已定位**的问题。还在猜的，明确标为「未定位」。
> - **被实验推翻的假设不删除**，用 ~~删除线~~ 加「已推翻」保留，并写清为什么错（见 §5）。
> - 每条都带**提交号**，可以 `git show <hash>` 回溯。

---

## 0. 怎么用这份文档

- 遇到新问题 → 在 §3 追加一条，编号 `P-xx`（连续递增，不复用）。
- 每条必须包含七个字段：**现象 / 影响 / 诊断过程 / 根因 / 修法 / 教训 / 提交**。
- 「诊断过程」比「修法」更重要 —— 它记录的是**怎么找到的**，这才是可迁移的能力。
- 如果一个问题跨多个模块，归到最主要的那一类，并在文末交叉引用。

---

## 1. 统计（截至 2026-09-19）

| 分类 | 条数 | 编号 |
|---|---:|---|
| A. 工具链 / 构建 | 4 | P-01 ~ P-03、P-20 |
| B. 编码 / 环境 | 4 | P-04 ~ P-07 |
| C. 硬件诊断方法 | 4 | P-08 ~ P-11 |
| D. 固件运行时 | 2 | P-12、P-13 |
| E. 文档 / 原代码与现实不符 | 4 | P-14 ~ P-16、P-19 |
| **F. 语言语义差异（移植专属）** | 1 | P-17 |
| **G. 测试与验证方法本身的问题** | 5 | P-18、P-21、P-22、P-23、P-24 |
| **合计** | **24** | |
| 其中：**我判断错过的** | 2 | H-1、H-2（见 §5） |

**按「根因在哪」分布**（这才是最有价值的一栏）：

| 根因 | 条数 | 说明 |
|---|---:|---|
| 我自己的代码 / 脚本 / 测试有 bug | 8 | P-08、P-09、P-11、P-13、P-18、P-20、P-22、**P-24** |
| 工具链 / 框架的隐式行为 | 5 | P-01、P-03、P-06、P-07、P-12 |
| 环境编码 / 平台差异 | 4 | P-02、P-04、P-05、P-10 |
| 语言语义差异（Python ↔ C） | 1 | P-17 |
| 硬件寄存器位域理解错误 | 1 | **P-21** |
| 参考值环境与真实环境不一致 | 1 | **P-23** |
| 原始材料不准确 | 2 | P-14、P-15 |
| 原代码固有缺陷 | 2 | P-16、P-19 |
| *（另计：我判断错过的假设）* | *2* | *H-1、H-2* |

> **只有 2 条（P-14、P-15）是"原始材料骗了我们"**，另 2 条是原代码本来就有的缺陷，
> 其余 **20 条全都是工具链、语言语义、硬件细节，或者我们自己造成的**。
> 这个分布本身就是教训：接手项目时，最大的风险往往不是别人留下的代码，而是
> 自己对新工具链、新语言语义和新硬件细节的想当然。
>
> ⚠️ 更要紧的是：**G 类（验证方法本身出错）已经有 5 条**，而且 P-21 与 P-23
> 是**同一个坑的两次出现** —— "我的参考值和被测代码犯同一个错，于是测试全绿"。
> 到 P2/P3 阶段，真正危险的不再是"代码写错"，而是**"我以为我验证过了"**。

---

## 2. 提炼出的方法论（比具体问题更值钱）

这 19 条是从下面 24 个具体问题（外加 §5 里 2 个被推翻的假设）里抽出来的。
**先读这节，再看具体条目。**

### 调试策略

1. **大批量自动化之前，先用一个已知答案做自检。**
   P-09 是最典型的：一个"自检"步骤如果早 10 分钟加上，就能省掉一整轮错误结论。
   自检的形式：拿一个**你已经知道结果**的输入，跑同一段代码，看结果对不对。

2. **探测"存在性"必须用权威判据，不能用"操作没报错"。**
   P-08 里 `readfrom_mem` 不报错 ≠ 器件存在。要用真正判断 ACK 的接口。
   推广：任何"成功/失败"判断，先问一句「这个判据能不能被伪造？」

3. **排查性能问题，第一步是找"同类实现的权威对比"。**
   P-12 能破案，靠的是「MicroPython 扫同一条总线只要 28 ms」这个对比。
   它一次性排除了"硬件就是慢""总线有问题"等所有猜想，把范围锁死在代码。
   **没有对比数字，就只能猜。**

4. **API 参数不起作用时，去读源码，不要连续猜。**
   P-12 我猜了两次都错（改 `ticks_to_wait`、改 `i2c_set_timeout`）。
   读源码 5 分钟定案。**猜两次不中就该换方法。**

5. **让故障可观测：加耗时、加进度、加"预期值断言"。**
   P-12 的定位全靠我给每个探测打印了微秒耗时 —— 那个"正好 1000 ms"是破案关键。
   如果只打印"成功/失败"，这个问题会拖很久。

### 工程习惯

6. **`except: pass` 是 bug 温床。** 诊断代码里的错误必须打印出来。
   P-11 里所有"释放总线"都在静默失败，还让我误判了两轮。

7. **主机端超时 ≠ 设备端停止。** P-10：主机把脚本杀了，板子还在跑。
   设备必须能被**外部**复位（硬复位引脚 / DTR-RTS），不能只依赖软协议。

8. **长任务要能"边跑边看"。** P-07：管道缓冲会让你在超时后一个字都拿不到。
   跑之前就问：这个任务失败时，我能看到多少信息？

9. **配置文件保持纯 ASCII。** P-01：一个中文注释就能让整个构建配置阶段崩掉。
   凡是被**其他语言的工具**读取的文件，默认按 ASCII 写。

10. **改动默认值后要清缓存。** P-03：`sdkconfig.defaults` 只对**尚未生成**的配置文件生效。
    这类"默认值 vs 已有值"的优先级问题在几乎所有构建系统里都存在。

### 心态与流程

11. **假设要写成"可以被证伪的实验"，并保留被推翻的痕迹。** 见 §5。
    写下来 + 做实验 = 一条命令解决争议；只在脑子里想 = 反复来回。

12. **文档 ≠ 现实。以代码和实测为准。**
    P-14 论文声称的功能大量不存在；P-15 参考电路图和本机不符。
    接手项目**先跑起来看真实行为**，再读文档。

### 迁移、移植与验证（P1/P2 新增，对 P2~P6 全都适用）

13. **移植数值代码时，先清点"语言语义差异"，而不是先清点函数签名。**
    `//`、`%`、`int()`、`round()` 在负数上的行为，Python 和 C **不一样**，
    而且**不会报任何错**（P-17）。搬代码前先扫一遍这几个运算符。

14. **"测试通过"必须配一句"这个测试怎么才会 FAIL"。**
    说不出它失败的路径，就不知道它到底测了什么 —— P-18 里我的测试因为输入太对称，
    对要验证的映射**完全不敏感**，却报了一串 PASS。**虚假的信心比 bug 更危险。**

15. **移植时编译器警告可能是上游代码 bug 的线索，别急着用 `(void)` 压掉。**
    P-19：`unused variable 'AB4_y'` 顺藤摸瓜，查出原实现有一段算了但从不使用的
    无效计算，以及一个"对输出毫无影响"的参数 `w`。**用"删了 golden 误差一位不差"
    来实证它无效** —— 这类实验成本极低，但证据强度极高。

16. **"读回来是对的"不等于"写对了"—— 编解码要有独立的第二个来源。**
    P-21：驱动把 `off=4096` 写成 `& 0x0F` 丢了 bit4，而我的回读解码器**犯了同一个错**，
    于是回读永远"通过"。这个 bug 活过了整个 P0 验收。
    ⇒ 验证一条写-读链路时，编解码**不能都出自你自己**；这次是"原 MicroPython 代码"
    充当了独立的第二来源。
    ⇒ 顺带：**碰硬件位域时数清楚位数**（12 位数据 + 1 位标志 = 13 位，掩码至少 5 位）。

17. **"预期不符"时，先看偏差的"量级"，再决定怀疑谁。**
    P-22：偏差 127 个计数单位 —— 浮点误差最大只能是 1，**量级本身在说
    "输入不一样"，而不是"算法不一样"**。先怀疑测试的输入，再怀疑代码。
    ⇒ 配套规矩：**凡是"参考值用到的输入"，都必须由参考实现自己产生**，
    不能在生成器里另算一份（手填的输入列 = 将来某天的假 FAIL，甚至假 PASS）。
    ⇒ 这类 bug 只在**重跑**时暴露，所以"改完必须重跑全套"不是形式主义。

18. **参考值必须在"真环境"里跑，而不是在我用手术刀切出来的环境里跑。**
    P-23：我为了"只抠出纯函数"而手工拼了一个命名空间，结果把 `padog.py`
    **59 项的默认值注入表**一起丢掉了 —— 参考值于是和 C 版**错在同一个地方**
    （都把小腿曲线偏置算成 0.375，真值是 0.25），测试全绿却都是错的。
    ⇒ 判断标准一句话：**如果参考值和被测代码都出自我的理解，
    那这个测试只是在测"我前后是否一致"。**
    ⇒ 手搭命名空间、手填输入列、手工"抠"函数 —— 每一次手术都有这个风险。
    ⇒ 现在的做法：`servo_output` 与 `control_chain` 两套参考值**都直接 exec
    真版 `padog.py`**，`hip`/`cs` 这类中间量也由真版函数算出来再写进 CSV。

19. **"我没搜到" ≠ "它不存在"。**
    P-24：我用 `^walk_faai` 搜不到定义，就判断那个默认值是凭空填的、准备改掉它 ——
    实际它藏在 `padog.py:80` 一个**元组字面量**里，锚定 `^` 直接漏报；
    而漏报看起来和"确实没有"一模一样。
    ⇒ **要断言某个值，就把它算出来**：`ns['_walk_faai']()` 一行代码定案，
    比任何搜索都可靠。（P-21/P-23 是同一条原则的另外两面：
    **让权威来源说话，而不是让我的工具说话。**）

---

## 3. 问题清单

### A. 工具链 / 构建

---

#### P-01 `sdkconfig.defaults` 里的中文注释导致构建配置阶段崩溃

| | |
|---|---|
| **现象** | `pio run` 报 `UnicodeDecodeError: 'gbk' codec can't decode byte 0xa2 in position 6`，然后 `CMake Error ... Failed to run kconfgen`，配置阶段失败 |
| **影响** | 工程完全无法构建 |
| **诊断过程** | 看 traceback 最后一帧：`kconfgen/core.py` 在 `for line_num, line in enumerate(f_in, start=1)` 处崩。它读的是 `--defaults ...sdkconfig.defaults`。文件是 UTF-8，而报错说 GBK 解码 → 说明 kconfgen 用**系统 locale 编码**打开文件 |
| **根因** | 中文 Windows 的 locale 编码是 GBK。ESP-IDF 的 `kconfgen` 用默认编码读 kconfig 文件，遇到 UTF-8 多字节序列就崩 |
| **修法** | `sdkconfig.defaults` 与 `platformio.ini` 全部改成**纯 ASCII**（英文注释）；中文说明移到 `README.md`。并用脚本验证：`non-ASCII bytes: 0` |
| **教训** | 被其他语言工具读取的配置文件，一律写 ASCII。这不是风格问题，是正确性问题 |
| **提交** | `0514755` |

---

#### P-02 `%u` 配 `uint32_t` 被 `-Werror=format` 拦下（真 bug）

| | |
|---|---|
| **现象** | `error: format '%u' expects argument of type 'unsigned int', but argument 10 has type 'uint32_t' {aka 'long unsigned int'}`，编译失败 |
| **影响** | 编译不过 |
| **诊断过程** | 报错指向 `ESP_LOGI(TAG, "... 周期 %u us", ..., s_period_us)`。`s_period_us` 声明为 `uint32_t`，在 ESP32 上就是 `unsigned long` |
| **根因** | **真 bug**（32 位平台上 `uint32_t` 与 `unsigned int` 宽度相同但不是同一类型，在 64 位或其他平台会真的出错） |
| **修法** | 加显式强转 `(unsigned)s_period_us`。**保留 `-Werror=format`** —— 我一开始想用 `build_unflags = -Werror` 绕过去，那是错的，这是免费的 bug 检测 |
| **教训** | 格式警告别压，改代码。要"绕开"编译警告时，先问自己：这个警告在别的平台上会不会是真错误？ |
| **提交** | `0514755` |

---

#### P-03 Flash 尺寸不匹配：`sdkconfig.defaults` 没生效

| | |
|---|---|
| **现象** | `Warning! Flash memory size mismatch detected. Expected 4MB, found 2MB!` |
| **影响** | 只分配 1 MB 给 app 分区，用不满 4 MB Flash（对后续放 Web 资源有影响） |
| **诊断过程** | 我已在 `sdkconfig.defaults` 加了 `CONFIG_ESPTOOLPY_FLASHSIZE_4MB=y`，但警告还在。直接查生成的配置文件：`Select-String FLASHSIZE sdkconfig.esp32dev` → `CONFIG_ESPTOOLPY_FLASHSIZE_2MB=y`。**说明我加的默认值根本没生效** |
| **根因** | `sdkconfig.defaults` 只在**生成** `sdkconfig` 时作为默认值使用；一旦 `sdkconfig.esp32dev` 已存在，**已有值优先**。我第一次构建时文件已经生成（值是 2MB），之后改默认值不起作用 |
| **修法** | 删掉 `firmware/sdkconfig.esp32dev` 重新构建 → 变成 `CONFIG_ESPTOOLPY_FLASHSIZE="4MB"`。并把它加进 `.gitignore`（新克隆的仓库本来就没有这个文件，所以默认值会正常生效） |
| **教训** | "默认值 vs 已生成文件"的优先级问题在几乎所有构建系统里都有。改了默认值 → 先清缓存再下结论 |
| **提交** | `0514755` |

---

#### P-20 `run_golden.bat` 被写到了仓库根目录（而且被我提交了进去）

| | |
|---|---|
| **现象** | 仓库根目录凭空多出一个 **0 字节**的 `run_golden.bat`；`tools/golden/` 下的那个才是真的 |
| **影响** | 别人在根目录看到同名文件，双击没反应；更糟的是**我把它提交了**（`355aa35`） |
| **诊断过程** | 写文件时我用的路径是相对的 `tools\golden\run_golden.bat`，但那次调用里进程的**当前目录**并不是仓库根 —— 于是文件落到了别处。事后 `git status` 才发现 |
| **根因** | **相对路径 + "我以为的当前目录"**：PowerShell 的 `$PWD`、.NET 的 `Environment.CurrentDirectory`、以及调用方实际的工作目录**是三个可能不一致的东西**（这次就是后者与前者不一致）。而且 `.bat` 不换行也能创建成功，所以它是 0 字节的空文件，不报错 |
| **修法** | 删除该文件（`bb45e82`）。**规矩：凡是写文件，一律用绝对路径**，不依赖任何"当前目录" |
| **教训** | ⭐ 1. **0 字节文件是最危险的产物** —— 它既"成功"了又完全没用，只有 `git status` 会告诉你<br>2. **相对路径永远是一个假设**，而这个假设经常不成立。写文件用绝对路径是零成本的<br>3. **`git add -A` 之后必须看 `git status`**，这次就是靠它抓到的 |
| **提交** | `355aa35`（误提交）→ `bb45e82`（删除） |

---

### B. 编码 / 环境

---

#### P-04 PowerShell 读文本文件乱码（UTF-8 + CR-only 换行）

| | |
|---|---|
| **现象** | 读学长论文的提取文本，`Get-Content` 输出 `鏈綉绉曟瘯涓氳璁?` 这类乱码；而且 `read` 工具报告"总共 1 行" |
| **影响** | 完全读不了论文内容 |
| **诊断过程** | 先看原始字节：`first 32 bytes: 2F 0D 0D E6 9C AC E7 A7 91 ...`。`E6 9C AC` = "本" 的 UTF-8 编码 → **文件是 UTF-8**。再看换行：`LF count: 0`、`CR count: 872` → **只有 CR，没有 LF**，所以整个文件被当成一行 |
| **根因** | 两个独立问题叠加：(1) 该文件是 CR-only 换行，`Get-Content` 不拆分；(2) 输出经过控制台时按 GBK 解码，把 UTF-8 显示成乱码 |
| **修法** | 用 .NET 显式指定编码读，再统一换行：`[IO.File]::ReadAllText($f,[Text.Encoding]::UTF8)` → `-replace "\r\n","\n" -replace "\r","\n"` → 写回 UTF-8。之后用 `read` 工具（按 UTF-8 读）查看 |
| **教训** | 处理来路不明的文本，**先看字节和换行统计**，不要直接读。乱码时先分清是"文件本身编码"还是"显示解码"问题 —— 这两者的修法完全不同 |
| **提交** | 未单独提交（一次性数据处理） |

---

#### P-05 本机无视觉能力，读不了电路图

| | |
|---|---|
| **现象** | `read_image`：`model "deepseek-v4-flash" does not declare image input`；`modlens_read_image`：底层模型同样无视觉，返回「完全不可读」；`describe_image`：`baseURL must be an absolute http(s) URL`（未配置） |
| **影响** | 无法直接看用户提供的电路图、截图 |
| **诊断过程** | 三条视觉通路逐个试，确认全不可用。然后转向本机能力：`tesseract` 未装、Python 无 OCR 库，但 **Windows 自带 OCR 引擎 + 中文语言包已安装**（`Get-WinUserLanguageList` → `zh-Hans-CN`） |
| **根因** | 当前模型是纯文本模型，环境里也没有配置视觉桥接服务 |
| **修法** | 用 PowerShell 5.1 调用 WinRT 的 `Windows.Media.Ocr`：先 2× 放大 + 2×2 重叠分块扫全文，再对关键区域 **4×/6× 定向放大 + word 级坐标**识别。电路图关键网络标号就是靠 6× 逐行 OCR 钉死的 |
| **教训** | 环境能力不足时，先找**操作系统自带**的能力顶上去，而不是立刻说"做不到"。OCR 这种能力 Windows/macOS 都内置 |
| **提交** | 未单独提交（脚本在 `%TEMP%`）；成果写入 `硬件实物核对清单.md` 附录 F |

---

#### P-06 Python 往 Windows 控制台写 UTF-8 抛 `UnicodeEncodeError`

| | |
|---|---|
| **现象** | 抓串口日志的脚本跑到一半崩：`UnicodeEncodeError: 'gbk' codec can't encode character '\ufffd'`，**把后面所有日志都截断了** |
| **影响** | 第一次抓取拿不到 P0 自检输出（正好截在关键位置之前） |
| **诊断过程** | 脚本逐块 `sys.stdout.write(chunk.decode('utf-8','replace'))`。替换字符 `\ufffd` 无法用 GBK 编码 → 崩 |
| **根因** | Windows 上 Python 的 stdout 默认按系统 ANSI（这里是 GBK）编码 |
| **修法** | 脚本开头 `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`；另外用 `codecs.getincrementaldecoder("utf-8")` 处理多字节字符被分包切断的情况 |
| **教训** | 任何"打印外部文本"的脚本，**开头就固定输出编码**。这类 bug 只在遇到特定字符时爆发，测试时很容易漏过 |
| **提交** | `5a7dc85`（`capture_boot_log.py`） |

---

#### P-07 长任务被超时杀掉后，一个字都拿不到

| | |
|---|---|
| **现象** | 全引脚扫描脚本超时后输出 `(no output)`；另一个长脚本只回了 `WROTE ...` 而没有内容 |
| **影响** | 白跑一轮，还得重跑 |
| **诊断过程** | 命令是 `... | Out-String -Width 200`。PowerShell 的 `Out-String` 会**把全部输出缓冲到管道结束**才输出；进程被超时杀掉 → 缓冲丢失 → 什么都拿不到 |
| **根因** | 管道格式化 cmdlet 的缓冲语义 + 进程被强杀 |
| **修法** | 长任务三选一：(1) 不接 `Out-String`，直接流式输出；(2) 放后台任务再轮询；(3) 用 `Select-String -Context` 边过滤边输出，并把结果写文件 |
| **教训** | 跑之前先问：**这个任务万一失败/超时，我能看到多少信息？** 长任务要设计成"边跑边看" |
| **提交** | 未单独提交（操作习惯） |

---

### C. 硬件诊断方法

---

#### P-08 `readfrom_mem` 假阳性，让"器件存在性"判断失效

| | |
|---|---|
| **现象** | 全引脚扫描报告 `SCL=5/SDA=21`、`SCL=22/SDA=21`、`SCL=32/SDA=21` 都"命中"两片 PCA9685。但 **GPIO5 和 GPIO32 根本没接到 PCA9685 的时钟线上** |
| **影响** | 扫描结论完全不可信，差点据此得出错误结论 |
| **诊断过程** | 注意三次命中的**共同点是 `SDA=21` 恒定，而 SCL 可以是任意引脚**。SDA 对上但 SCL 没接，器件不可能应答 → 说明"读成功"这个判据本身不可靠。换用 `i2c.scan()`（真正判断地址 ACK）后，只有 `SCL22/SDA21` 命中 |
| **根因** | MicroPython 1.13 的 `i2c.readfrom_mem()` 在总线无器件时**可能不抛异常而返回残留数据**。（`PA_IMU.py`/`PA_STABLIZE.py` 里用的就是这个接口） |
| **修法** | 探测器件一律用 `i2c.scan()`；C 版一律看 `esp_err_t` 返回值，不依赖"读到的内容" |
| **教训** | **探测存在性要用权威判据。** 判断标准是"器件 ACK"，不是"操作没报错"。看到可疑结果时，找**所有命中项的共同点** —— 这次就是靠 SDA 恒为 21 破的 |
| **提交** | `0514755`（`diagnostics/board_find_i2c.py` 保留为反面教材） |

---

#### P-09 扫描每对引脚只测一个方向，漏掉正确答案

| | |
|---|---|
| **现象** | 第一轮全引脚扫描**连已知有器件的 21/22 都没扫到**，得出"总线上什么都没有"的错误结论 |
| **影响** | 差点误判"IMU 不存在" |
| **诊断过程** | 加了"自检"步骤：对已知有器件的 `SCL22/SDA21` 先扫一遍。自检**通过**了 → 说明方法本身有效 → 那批量结果为空就自相矛盾。再看代码：循环用 `for i<j` 生成 `(pins[i], pins[j])`，**每对只测一个方向**（第一个当 SCL、第二个当 SDA）。而 `21/22` 这对生成的顺序是 `scl=21, sda=22` —— **接反了** |
| **根因** | I2C 的 SCL 与 SDA **不可互换**。正确方向 `SCL=22/SDA=21` 从未被测过 |
| **修法** | 每对引脚测**两个方向**（有序组合，而非组合） |
| **教训** | ⭐ **这是本次最有价值的一条：大批量自动化之前，先用已知答案做自检。** 如果一开始就有自检，这个 bug 会在 10 秒内暴露，而不是浪费一整轮 |
| **提交** | `0514755` |

---

#### P-10 `mpremote` 进不去 raw REPL（板子还在跑上一个脚本）

| | |
|---|---|
| **现象** | `mpremote.transport.TransportError: could not enter raw repl`，之后任何 `mpremote` 命令都失败 |
| **影响** | 板子"失联"，无法继续调试 |
| **诊断过程** | 想起上一轮的长时间扫描脚本是**主机端被超时杀掉**的 —— 主机放弃了，但**板子还在继续跑那个循环**，所以 REPL 不响应 |
| **根因** | 主机端超时 ≠ 设备端停止 |
| **修法** | 用 pyserial 直接操作串口的 DTR/RTS 引脚做硬复位（CH340 + ESP32 DevKit：`RTS→EN`、`DTR→IO0`）。序列：`IO0=HIGH` → `EN=LOW` 保持 0.2s → `EN=HIGH`。完全不依赖 REPL 协议 |
| **教训** | 调试设备时，**必须有一条不依赖被测软件的外部复位通路**。写进 `diagnostics/hard_reset.py` 常备 |
| **提交** | `0514755` |

---

#### P-11 MicroPython 1.13 没有 `I2C.deinit()`，而我的错误处理把它藏起来了

| | |
|---|---|
| **现象** | 扫描脚本反复创建 I2C 失败却毫无提示，结果"什么都没扫到" |
| **影响** | 直接导致 P-09 那一轮的错误结论（两个 bug 叠加） |
| **诊断过程** | 单独测一次：`AttributeError: 'I2C' object has no attribute 'deinit'`。而我所有释放总线的地方都写成 `try: i2c.deinit() except Exception: pass` —— **全部静默失败** |
| **根因** | 两个问题：(1) 该固件版本的 `I2C` 没有 `deinit`；(2) `except: pass` 把错误吞了 |
| **修法** | 不再依赖 deinit，改用"每轮重新创建 + 权威 `scan()` 判定"；诊断代码里的异常一律打印 |
| **教训** | **`except: pass` 是 bug 温床。** 诊断/实验代码尤其要打印错误 —— 你正在做的事就是"搞清楚发生了什么"，此时隐藏信息等于自毁 |
| **提交** | `5a7dc85`（这轮开始打印错误并计数） |

---

### D. 固件运行时

---

#### P-12 ⭐ 开机自检卡死：I2C 全总线扫描 >40 秒（本日最硬的一个）

| | |
|---|---|
| **现象** | `pio run -t upload` 成功后，串口日志停在 `p0: 步骤 2/4：扫描总线` 再也不动，40 秒无输出 |
| **影响** | P0 无法验收；看起来像"初始化卡死" |
| **关键对比** | **MicroPython 版扫同一条总线只要 28 ms**（本会话早前实测） |
| **诊断过程（三次才对）** | ① 怀疑 `ticks_to_wait` 太大 → 从 20ms 改成 10ms → **无效**（仍 ~1000ms/地址）<br>② 怀疑硬件 SCL 超时 → 加 `i2c_set_timeout(80000)`，日志显示默认值竟是 `8000`（=100µs），改完**仍然无效**（还是 1000991 µs）<br>③ **去读驱动源码** `components/driver/i2c/i2c.c` → 找到根因 |
| **根因（源码级）** | ```c<br>#define I2C_CMD_ALIVE_INTERVAL_TICK (1000 / portTICK_PERIOD_MS)<br>```<br>`i2c_master_cmd_begin()` 的事件等待循环里把这个值作为**下限强制抬升**：<br>```c<br>if (wait_time < I2C_CMD_ALIVE_INTERVAL_TICK) {<br>    wait_time = I2C_CMD_ALIVE_INTERVAL_TICK;   // 恒 1000 ms<br>}<br>xQueueReceive(p_i2c->cmd_evt_queue, &evt, wait_time);<br>```<br>于是：**器件 ACK → DONE 事件立刻到达（273~491 µs）；器件 NACK → 根本不产生事件 → 只能干等 1000 ms 报 `ESP_ERR_TIMEOUT`**。112 个地址 × 1 s = **112 秒**。而这个公式 `1000 / portTICK_PERIOD_MS` 保证**永远是 1 秒**，跟 tick 频率无关 |
| **为什么 MicroPython 快** | MicroPython 1.13 跑在 **IDF 3.3.2** 上，那版驱动没有这个 ALIVE 下限。**同一份逻辑，换 IDF 版本行为完全不同** |
| **修法** | 全总线扫描改用 **GPIO 位操作**，在 I2C 驱动**安装之前**做（那时驱动还没接管引脚，1 秒下限不适用）：`bsp_i2c_bitbang_begin()` 把 SCL/SDA 设为开漏 + 上拉，`bsp_i2c_scan_bitbang()` 对每个地址发 START + 地址 + 读 ACK + STOP。**112 个地址 / 21 ms**，比 MicroPython 的 28 ms 还快 |
| **教训** | ⭐ 三条：<br>1. **性能问题先找权威对比数字**（28 ms vs 112 s）—— 它一次性排除了所有"硬件就是慢"的猜想，把范围锁死在代码；没有对比就只能猜<br>2. **API 参数不起作用时去读源码，不要连续猜** —— 我猜了两次都错，读源码 5 分钟定案<br>3. **迁移到新框架时，旧代码的"常识"可能不成立** —— 同一条扫描逻辑换 IDF 版本行为天差地别。这条对后面 P1~P6 全都适用：**任何 I2C 错误路径都带 1 秒惩罚**，设计超时/重试/看门狗时必须算进去 |
| **附带收获** | 加了 `probe_timed()`（每探测打印微秒耗时）和进度日志 —— **正是那个"正好 1000 ms"的数字破的案**。如果只打印成功/失败，这问题会拖很久 |
| **提交** | `5a7dc85`（加可观测性）→ `73a0b6e`（先让开机有界）→ `e400481`（位操作扫描，彻底解决） |

**最终对比表:**

| 扫描方式 | 112 个地址耗时 |
|---|---|
| IDF 5.1.2 驱动路径（修复前） | **112 000 ms** |
| MicroPython 1.13（IDF 3.3.2） | 28 ms |
| **C 版位操作（现在）** | **21 ms** ✅ |

---

#### P-13 串口命令回显只有命令名，看不到参数

| | |
|---|---|
| **现象** | 日志显示 `> raw`，而不是 `> raw 0 FE`，无法从日志确认到底执行了什么 |
| **影响** | 排障时看不出实际参数 |
| **诊断过程** | 看 `handle_line()`：为了拆出命令和参数，在第一个空格处写入 `'\0'`，之后却用被截断的 `cmd` 回显 |
| **根因** | 字符串原地拆分后丢了原始内容 |
| **修法** | 拆之前先 `strncpy` 一份整行副本专用于回显 |
| **教训** | 日志要能**完整复原**当时发生了什么。回显被截断的输入，等于日志不可信 |
| **提交** | `e400481` |

---

### E. 文档与现实不符

---

#### P-14 论文/PPT 声称的功能，代码里大量不存在

| | |
|---|---|
| **现象** | 论文写：改进型 CPG 中枢模式发生器、卡尔曼滤波多传感器融合、三自由度**串联弹性**驱动、关节编码器、足端压力传感器、4 自由度 SCARA 机械臂、OV2640 双舵机云台 |
| **实际（读代码后）** | 硬编码三角函数轨迹；四元数互补滤波（且 `PA_STABLIZE.stab()` 在主循环里被注释掉）；刚性舵机开环，无任何力/位置反馈；机械臂只有 2 舵机 + 夹爪；摄像头代码里没有任何云台舵机 |
| **影响** | 如果按论文架构规划迁移，会完全跑偏 |
| **诊断过程** | 逐行读 `padog.py` / `PA_TROT.py` / `PA_WALK.py` / `PA_IMU.py` / `PA_STABLIZE.py` / `mech_arm.py`，与论文逐项对照 |
| **根因** | 毕业论文/答辩 PPT 存在"理想架构"描述与实际实现脱节（这在毕设里很常见） |
| **修法** | 建立原则：**以代码和实测为准，论文只作对照**。`HANDOFF.md` 第 0 节开头即写明此警告；迁移表也标注"不按论文中的理想架构假设" |
| **教训** | 接手项目，**先跑起来看真实行为**，再读文档。文档是"作者希望你相信的"，代码是"实际发生的" |
| **提交** | `18785a1`、`1fc020c` |

---

#### P-15 参考电路图与实物不符

| | |
|---|---|
| **现象** | 用户提供的电路图（灯哥菠萝狗官方课件）把 **GPIO21/22 标给 MPU6050**、18/19 标给 PCA9685 |
| **实际** | 实测 21/22 上是两片 PCA9685（`0x40`/`0x41` 应答），18/19 全空 |
| **影响** | 一度据此产生"IMU 藏在 18/19"的错误假设（见 H-2） |
| **诊断过程** | 用 Windows OCR 逐行 6× 放大读出网络标号 → 得到 `pin22 → MPU-SCL`、`pin27 → PCA-SCL` 等映射 → 与实测冲突 → 实测优先 |
| **根因** | 这是**官方参考设计**的图，不是这台改装机的接线图 |
| **修法** | 明确边界：参考图只能用于**理解设计意图和电源拓扑**，**绝不能用来推断本机引脚分配**。这条写进 `硬件实物核对清单.md` §0.2.1 |
| **教训** | **参考设计 ≠ 你这台机器。引脚必须实测。** 同时：这张图仍然有价值 —— 它的电源树部分（`XL4005 → VCC-5V → AMS1117 → VCC-3.3`）给了排查方向 |
| **提交** | `0514755` |

---

#### P-16 `padog.do_connect_STA()` 是无超时死循环

| | |
|---|---|
| **现象** | 硬复位后日志停在 `connecting to network...` 不动，连 REPL 都进不去（只能硬复位救回来） |
| **影响** | 热点不在时，板子开机即卡死 |
| **诊断过程** | 复位抓启动日志时看到的；随后读 `padog.py` 定位 |
| **根因** | ```python<br>while not wifi.isconnected(): pass<br>```<br>没有超时、没有重试上限 |
| **修法** | **尚未修**（属于 C 版 P5 的任务）。已记入 `HANDOFF.md` 与迁移表：C 版 `comm/wifi.c` 必须有连接超时 + 失败降级（AP 模式或停车待命） |
| **教训** | **任何"等待外部条件"的循环都必须有超时。** 外部条件（网络、传感器、人工操作）永远可能永远不满足 |
| **提交** | 记录于 `0514755` |

---

#### P-19 编译器警告指认出原代码里的「无效计算」和「隐藏全局」

| | |
|---|---|
| **现象** | 移植 `PA_ATTITUDE.cal_ges()` 时，gcc 报 `warning: unused variable 'AB4_y'`；移植 `PA_TROT.py` 时发现只有两个分支、没有 `else` |
| **影响** | 不致命，但**照抄一个无效计算进 C 版会让人以为它有用**，后面不敢删、不敢改；`Ts`/`faai` 是模块级全局则意味着**改一次参数会影响所有调用点** —— 在单线程 MicroPython 里勉强能跑，在多任务 FreeRTOS 里就是竞态 |
| **诊断过程** | ① 警告 → 去看 `AB1_y..AB4_y` 到底喂给了谁 → 返回的 `[x0..x3, y0..y3]` 里**根本没有它们**<br>② 反向追：这 4 个变量是 `w`（左右腿间距）**唯一**的使用者 ⇒ 推论 **`w` 对输出没有任何影响**<br>③ 验证推论：把无效计算删掉重跑 golden，**最大误差 0.000022257 mm 一位不差** ⇒ 实证其无效<br>④ `PA_TROT.cal_t()`：`if/if` 无 `else`，`t > Ts` 会 `UnboundLocalError`；`Ts`/`faai` 是模块级全局（隐藏状态） |
| **根因** | 原代码本身的设计缺陷，**不是移植的错** |
| **修法** | ① 删掉 `AB*_y` 计算，保留 `w` 形参以维持签名一致并加注释说明它是 no-op（附 golden 实证）<br>② 时序参数 `Ts`/`faai` 改成**显式入参** `gait_trot_cfg_t`<br>③ 对 `t<0 \|\| t>Ts` 做**相位回绕**，但 `t==Ts` 不回绕 —— 刻意与原实现保持一致 |
| **教训** | ⭐ 1. **编译器警告不只是在挑你的刺，有时它在指认上游代码的 bug。** 遇到"删了也不影响结果"的警告，先当成线索查一遍，别急着加 `(void)` 压掉<br>2. **"删掉后 golden 误差一位不差"是最硬的证据** —— 比读十遍代码都强。这类"证明某段代码无效"的实验，成本极低（改一行 + 跑一次），一定要做<br>3. **模块级可变全局是隐藏的接口**。移植到多任务环境时必须显式化成参数 |
| **提交** | `d631105`（body_pose）、`5d55999`（gait_trot） |

---

### F. 语言语义差异（移植专属，最隐蔽的一类）

---

#### P-17 ⭐ Python 的 `//` 和 `%` 与 C 的 `/` 和 `fmodf` 在负数上完全不同

| | |
|---|---|
| **现象** | 不报错、不崩溃、编译全过 —— **结果只是"有点不对"**。移植版算出来的轨迹和原版差一整个周期 |
| **影响** | 这是本次迁移中**最危险的一类 bug**：没有异常、没有警告、数值"看起来像那么回事"，只有逐数值对照才能发现 |
| **两处真实差异** | ① `filter_moving_avg.py`：`return self.cache[1] // (self.len - 3)`<br>② `PA_WALK.py`：`phi = local_t % T`，而 `local_t` 可能为负 |
| **根因（语言语义）** | **整除**：Python 的 `//` 向 **−∞** 取整（`-7 // 2 == -4`）；C 的 `/` 向 **0** 截断（`-7 / 2 == -3`）<br>**取模**：Python 的 `%` 结果**符号跟随除数**，恒非负（`-1 % 5 == 4`）；C 的 `fmodf` **符号跟随被除数**（`fmodf(-1,5) == -1`）<br>⇒ 两处都**恰好差 1**（差一个取整单位 / 差一整个步态周期） |
| **诊断过程** | 这次**不是试出来的，是读代码时提前预料到的** —— 看到 `//` 和 `%` 出现在"操作数可能为负"的位置，就去查语义。两个模块各写了一个复刻函数：`floor_div()`（`filter_moving_avg.c:39`）和 `py_fmodf()`（`gait_walk.c:55`） |
| **验证它真的重要（故意搞破坏实验）** | 把 `filter_moving_avg.c` 的向下取整修正抽掉（退化成 C 截断除法），重跑同一个 golden 测试：<br>```<br>FIRST MISMATCH: window=2 step=9 in=0 expected=-4 got=-3<br>mismatches  : 101 / 264<br>RESULT: FAIL<br>```<br>**264 项里 101 项不符，第一个错就在负数取整上。** 这说明修正不是摆设，也说明测试不是"假装通过" |
| **教训** | ⭐⭐ 1. **移植数值代码时，把 `//`、`%`、`int()`、`round()` 全部当成"待确认的语义差异"**，逐个问：操作数可能是负数吗？<br>2. **这类 bug 没有报错，所以"能编译能跑"完全不能作为验收标准** —— 必须逐数值对照<br>3. **`int()` 是向零截断，和 `//` 不是一回事**（`PA_WALK` 的重心副作用用了 `int()`，那几个输出是整数，golden 要求**精确相等**）<br>4. **写完修正要做"破坏性验证"**：故意删掉修正，确认测试会 FAIL。否则你不知道测试到底测没测到这条路径 |
| **提交** | `875a6ba`（filter）、`355aa35`（gait_walk） |

> **这条对整个 P2~P6 都适用**：只要还在从 MicroPython 搬代码，就先扫一遍这四个运算符。

---

### G. 测试与验证方法本身的问题

> 这一节的共同点：**出错的不是被测代码，而是"验证"这件事本身。**
> 到 P2/P3 阶段，真正危险的不再是"代码写错"，而是"我以为我验证过了"。
> （P-21 按根因属于硬件位域，但它是在这一节的工作中被逼出来的，故一并放在这里。）

---

#### P-18 ⭐ 我的对照测试一开始是"假的"：输入选得太对称，测不出真 bug

| | |
|---|---|
| **现象** | `gait_walk` 的 golden 测试第一版**全部 PASS**，但我随后发现它**根本测不出**我想测的那个东西 |
| **影响** | 如果没发现，我会拿着一个 PASS 的报告去说"移植正确"—— **这是比代码 bug 更严重的问题：虚假的信心** |
| **诊断过程** | 原实现 `cal_w()` 的形参顺序是 `(r1, r4, r2, r3)` —— 和腿的自然编号 `1,2,3,4` **不一致**（中间两项对调）。而我第一版参考值的腿系数写死成 `(1,1,1,1)`：<br>⇒ 映射对错、顺序颠倒，**算出来都一样** —— 测试对我要验证的性质**完全不敏感** |
| **根因** | **测试输入的设计失误**：用全等的输入去测"位置/顺序/映射"这类性质，等于没测。这是我自己在测试里挖的洞 |
| **修法** | 换成 **4 组互不相等的系数**，含 `(1, -2, -1, 0.5)` 这种非对称、含负数、量级不同的组合。这样只要映射错位，数值立刻对不上 |
| **教训** | ⭐⭐ 1. **"测试通过"必须配一句"这个测试为什么能失败"** —— 说不出它怎么才会 FAIL，就不知道它测了什么<br>2. **测"映射 / 顺序 / 索引"时，输入必须互不相等且非对称**（全 1、全 0、全相等都是无效输入）<br>3. **优先怀疑测试输入，而不是急着相信结果**。P-09（自检）和这条是同一个教训的两个面：**验证手段本身也要被验证** |
| **提交** | `355aa35` |

---

#### P-22 我的 golden 生成器往 CSV 里写了一列"参考值根本没用到的输入"

| | |
|---|---|
| **现象** | P2 的 `servo_output` 测试报 94 处不符、最大偏差 **127** 个计数单位。而同一批代码在 [A] 角度/占空比套件里 **284/284 精确通过** |
| **影响** | 差点让我去改**本来正确的** C 代码 —— 因为"参考值"和"C 版"看起来在同一组输入上算出了不同结果 |
| **诊断过程** | ① 偏差 127 太大，不像浮点问题（浮点最多差 1）→ 说明**输入不一样**，不是算法不一样<br>② 逐个核对 CSV 的输入列与参考实现真正读到的值 → 发现 `cs1..cs4`（爬行压低增量）那一列是我**自己填的随机数**<br>③ 而参考实现里 `servo_output` 是调用 `_crawl_shank_servodelta()` **自己算** cs 的，`crawl_phase=0` 时恒为 0 —— 它压根没看我填的那一列 |
| **根因** | **CSV 里声明的输入 ≠ 参考值实际使用的输入。** 这是"测试自己的 bug"，不是代码的 bug。和 P-18（输入太对称）是同一类错误的另一面：那次是**输入没有区分度**，这次是**输入不对应** |
| **修法** | 把 `cs` 从"我自己填"改成"**调用参考实现的 `_crawl_shank_servodelta()` 算出来再写进 CSV**"——于是 CSV 里的值必然等于参考值用到的值。改完立刻 720/720 精确相等 |
| **教训** | ⭐⭐ 1. **"预期不符"时先怀疑测试，再怀疑代码。** 尤其当偏差**大得不像数值误差**时（127 vs 1 个计数单位）—— **量级本身就是线索**<br>2. **凡是"参考值用到的输入"，都必须由参考实现自己产生**，不能在生成器里另算一份。手填的输入列 = 未来某天的假 FAIL（或者更糟：假 PASS）<br>3. 这类 bug **只会在重跑时暴露**，所以"改完必须重跑全套"不是形式主义 |
| **提交** | 随 P2 提交 |

---

#### P-21 ⭐ 一次位掩码吃掉了「全关」标志：从 P0 就存在的真 bug，只插 USB 永远看不出来

| | |
|---|---|
| **现象** | 做 P2 的 golden 对照时，参考值里 `duty=0` 那一行是 `pwm(index, 0, **4096**)`，而 C 版算出来的是 `(0, 0)` |
| **影响** | `drv_pca9685_all_off()` 号称"12 路无脉冲 / 舵机松力"，**实际写进寄存器的是 ON=0 OFF=0**。上电安全态、急停、超时停车**全都建立在这个动作上** |
| **诊断过程** | ① 参考值解码用的是 `(data[3] & 0x0F) << 8` → 4096 的高字节 0x10 被读成 0 → 我第一反应是"解码器写错了"<br>② 回头查 C 驱动 `drv_pca9685_set_pwm()`：**编码端也是 `& 0x0F`** —— 同一处错误两边都有，所以**光看日志根本对不出来**<br>③ 查 PCA9685 数据手册：`LEDn_OFF_H` 的 **bit4 = FULL OFF**，`LEDn_ON_H` 的 bit4 = FULL ON。4096 正是靠 bit4 表示，`& 0x0F` 恰好把它抹掉<br>④ 正确掩码是 **`& 0x1F`**（bits 0-3 = 计数高位，bit4 = 全开/全关）。0..4095 的正常占空比高字节最大 0x0F，所以对它们没有任何影响 |
| **根因** | 位域宽度搞错：12 位计数 + 1 个标志位，共 **13 位**，但只掩了 4 位 |
| **修法** | 编码端（`drv_pca9685.c`）与解码端（`tools/golden/mpy_stubs.py`）都改成 `& 0x1F`。真机 `readback` 立刻从 `OFF=0` 变成 `OFF=4096` |
| **为什么 P0 没发现** | ⭐ **没有负载就看不出来。** P0 只插 USB、不接电池，舵机根本不动，"松力"和"不明状态"在肉眼上完全一样；而 ON=0/OFF=0 在很多情况下**碰巧也是 0% 占空比**。**是逐位对照把它逼出来的** |
| **教训** | ⭐⭐ 1. **"没报错"和"看起来对"都不是验证。** 这个 bug 活过了整个 P0 验收（编译过、自检过、寄存器回读"成功"），因为回读用的解码器和编码器犯了同一个错<br>2. **写-读的编解码必须来自独立的第二个来源。** 这次是"原 MicroPython 代码"充当了那个独立来源<br>3. **碰硬件位域时数清楚位数**：12 位数据 + 1 位标志 = **13 位**，掩码至少要 5 位 |
| **提交** | 随 P2 提交 |

---

#### P-23 ⭐ 参考值和 C 版**错在同一个地方**：我的"参考值"跑在一个手搭的命名空间里

| | |
|---|---|
| **现象** | P2 的 `servo_output` golden 测试全绿（720/720 精确相等）。但在做 P3 全链路对照时，把 `servo_map_shank_bias()` 实际值算出来是 **44.625**，而真版 `padog.py` 环境里算出来是 **29.75** —— **差了 50%** |
| **影响** | 小腿舵机的二次拟合曲线偏置错了 ⇒ 所有走 IK 的姿态（站姿、步态）的小腿角都偏 ≈15°，舵机会把小腿压得比原版低。**而 P2 的"真机 12/12 与 golden 相同"验证也建立在错的参考值上**（那次恰好测的是"直接站姿"路径，不经过这条曲线，所以那 12 个数字本身仍然有效） |
| **诊断过程** | ① 偏置公式是 `extra = (l1+l2) - leg_len_ref`，乘一个 `per_mm`。原函数里 `per_mm = 0.375` 是兜底值，但上面有 `try: per_mm = float(shank_ik_bias_per_mm) except NameError: pass`<br>② 我用 `^shank_ik_bias_per_mm` 搜不到定义 → 以为走兜底 → 硬编码 0.375（**这里又踩了 P-24**）<br>③ 在真版命名空间里求值：`_shank_ik_bias()` 返回 **29.75** = `119 × 0.25` ⇒ `per_mm` 实际是 **0.25**，兜底值 0.375 是**死代码**<br>④ 根因找到：`padog.py` 第 57~81 行有个 **59 项的默认值注入表**，里面定义了 `("shank_ik_bias_per_mm", 0.25)`。而我的 P2 参考值是 `extract_padog_funcs()` **手工拼了一个命名空间**跑出来的 —— 那个命名空间里**没有跑这张注入表**，于是参考值也退到 0.375。<br>⑤ ⇒ **参考值与 C 版犯了同一个错，所以测试全绿。** |
| **根因** | **参考值的运行环境不是真环境。** 我为了"只抠出纯函数"而手工搭命名空间，结果连"模块级默认值注入"这种**真实存在的前置步骤**一起丢掉了。测试于是验证的是"我的理解 vs 我的实现"，而不是"原版 vs 我的实现" |
| **修法** | ① `servo_map_shank_bias()` 改为 **0.25**，导出 `servo_map_shank_bias_per_mm()` 供测试打印<br>② **`gen_servo_output()` 不再手工拼命名空间**，改成和 `control_chain` 一样**直接 exec 真版 `padog.py`**（`load_padog_ns()`）<br>③ 顺带把 `hip` 与 `cs` 也改成"由真版 `_hip_leg_deltas()` / `_crawl_shank_servodelta()` 算出来再写进 CSV"，而不是手填<br>④ 改完重跑：`servo_map` 仍然 1004/1004 精确相等，但 ch2 的占空比从 312 变成 **262** —— 证明这个修正真的改变了输出 |
| **教训** | ⭐⭐ 1. **这是 P-21 的同一个坑的第二次出现**：encoder 和 decoder 犯同一个错，测试就永远发现不了。P-21 是"我的解码器和驱动的编码器都写错掩码"，这条是"我的参考值和 C 版都用了错的常量"<br>2. **参考值必须在"真环境"里跑，不能在被我用手术刀切出来的环境里跑。** 只要我手工拼过命名空间、手工填过输入列、手工"抠"过函数，就有把真实前置步骤一起丢掉的风险<br>3. **判断标准很简单**：如果参考值和被测代码都出自**我的理解**，那这个测试只是在测"我前后是否一致"<br>4. 附带发现一个**通用做法**：凡是要断言某个中间量，就**在真环境里求值并打印出来**（`ns['_shank_ik_bias']()`），而不是靠读代码推断 |
| **提交** | 随 P3 提交 |

---

#### P-24 我差点根据一次"不完整的搜索"改掉一个**本来正确**的值

| | |
|---|---|
| **现象** | 读代码时发现 `app_config.c` 里 `walk_faai` 默认 0.30，而我用 `Select-String -Pattern "^walk_faai"` 在 `micropython/*.py` 里搜不到这个变量 —— 于是判断"这个值是我凭空填的，原版其实是 `faai*0.55 = 0.231`"，准备把它改掉 |
| **影响** | 如果真的改了，就把一个**正确**的默认值改成错的，而且改完之后所有 WALK 相关行为都会偏离原版 —— 这是一次"由我造成的回归" |
| **诊断过程** | 准备改之前先做了一次实证：直接在真版 padog 命名空间里求值 `_walk_faai()`，得到 **0.300**，不是 0.231 ⇒ 说明 `walk_faai` **确实被定义过**。<br>于是换掉锚定 `^` 重新搜：`Select-String -Pattern "walk_faai"` → 命中 `padog.py:80`，藏在**一个 59 项的默认值注入表**里：<br>```python<br>for _hk, _hd in (..., ("walk_faai", 0.30), ...):<br>    if _hk not in _g: _g[_hk] = _hd<br>``` |
| **根因** | **我的搜索模式有锚定，而定义在元组字面量里、不在行首。** `^walk_faai` 只匹配"行首就是 walk_faai"，于是**漏报**。而我拿"搜不到"当成了"不存在"——这是把"没找到"当成"没有"的经典错误 |
| **修法** | 不改 `walk_faai`（它是对的）。改的是**判断依据**：凡是"某变量是否存在/某值是什么"，**一律去真版命名空间里求值**，不用搜索下结论 |
| **教训** | ⭐⭐ 1. **"我没搜到" ≠ "它不存在"。** 搜索模式（锚定、大小写、是否跨行、是否在字符串里）每一条都能造成漏报，而漏报看起来和"确实没有"一模一样<br>2. **要断言某个值，就把它算出来。** `ns['_walk_faai']()` 一行代码定案，比任何搜索都可靠 —— 这和 P-21/P-24（命名空间那条）是同一条：**让权威来源说话，而不是让我的工具说话**<br>3. **改一个"看起来错"的值之前，先做一次能证伪的实验。** 这次如果先改后验，就得在 golden 报错时才回头，白跑一轮 |
| **提交** | 随 P3 提交 |

---

## 4. 反直觉"坑"速查表

遇到事情先扫一眼这张表。

| 症状 | 第一反应（常常是错的） | 真实原因 | 条目 |
|---|---|---|---|
| 编译配置阶段崩在 GBK 解码 | 文件坏了 | 配置文件里写了中文，工具按 locale 读 | P-01 |
| Flash 尺寸警告改不掉 | 参数名写错了 | 已生成的 sdkconfig 优先于 defaults | P-03 |
| 读文本乱码 | 文件编码错了 | 可能是 CR-only 换行 + 控制台 GBK 解码 | P-04 |
| 扫描"I2C 无器件" | 硬件坏了 | 可能每对引脚只测了一个方向 | P-09 |
| 扫描到的引脚数是乱的 | 器件真在那儿 | `readfrom_mem` 不报错 ≠ 器件存在 | P-08 |
| 脚本反复创建外设失败且无提示 | 硬件问题 | `except: pass` 把 AttributeError 吞了 | P-11 |
| 板子"失联"，mpremote 进不去 | 板子挂了 | 上一个脚本还在板上跑；主机超时没停设备 | P-10 |
| 长任务超时后毫无输出 | 脚本没跑 | 管道缓冲被进程强杀一起丢了 | P-07 |
| I2C 探测固定耗时正好 1 秒 | 总线慢/硬件问题 | IDF 驱动的 `I2C_CMD_ALIVE_INTERVAL_TICK` 下限 | P-12 |
| `i2c_set_timeout` 改了没用 | 参数单位错了 | 那条路径不受它控制，得读源码 | P-12 |
| 换框架后同样的代码行为完全不同 | 代码搬错了 | 底层版本变了（IDF 3.3 vs 5.1） | P-12 |
| 论文说有的功能代码里找不到 | 代码没写完 | 论文描述的是"理想架构" | P-14 |
| 参考图引脚和实测不符 | 接线接错了 | 参考图是通用设计，不是你这台 | P-15 |
| 移植后数值"差不多但不对"，且**无任何报错** | 浮点精度不够 | Python `//` / `%` 的负数行为与 C 不同，**恰好差 1** | **P-17** |
| 对照测试全 PASS，却漏掉了真 bug | 代码写对了 | 测试输入太对称（全 1），对映射/顺序不敏感 | **P-18** |
| 删掉一段代码，结果一点没变 | 没删干净 / 缓存 | 那段代码**本来就是**无效计算 | **P-19** |
| 仓库根目录凭空多出一个同名空文件 | 手滑 | 相对路径 + "我以为的当前目录"不是同一个目录 | **P-20** |
| 寄存器回读"成功"但硬件行为不对 | 硬件坏了 | **回读的解码器和编码器犯了同一个错** | **P-21** |
| 对照测试偏差大得不像浮点误差（127 而不是 1） | 算法写错了 | 测试输入与参考值实际用的输入不是同一个 | **P-22** |
| 测试全绿，但实测值与真实固件差 50% | 移植抄错了 | **参考值跑在一个手搭的命名空间里，漏掉了原版的默认值注入表** —— 参考值和 C 版错在同一个地方 | **P-23** |
| 搜不到某个变量的定义，于是准备"修正"它 | 那个值是凭空填的 | 搜索模式有 `^` 锚定，而它在元组字面量里 —— **漏报被当成了"不存在"** | **P-24** |

---

## 5. 被推翻的假设（诚实记录）

> 这一节保留我判断错误的过程。**不删的原因**：记下"我为什么猜错"，比记下"正确答案"更能防止再犯。
> 格式：假设 → 提出依据 → 证伪实验 → 结论 → 附带收获。

### H-1 ~~「不插电池时 PCA9685 会没电，所以扫不到 0x40/0x41」~~ **已推翻**

**提出依据**（看起来很有理）：
- 参考电路图里电源树是 `电池 → XL4005 → VCC-5V`，而 PCA9685 的逻辑 `VCC` 接 `VCC-5V`
- 用户明确说"我不插电池舵机没电"
- 又观察到使用 `scan` 时启动自检卡在扫描阶段

**推理链**：PCA9685 没供电 → 模块上的 I2C 上拉电阻也没了 → 总线只剩 ESP32 内部弱上拉（约 45k）→ 上升沿极慢 → 每次传输等超时 → 112 个地址要几分钟。**链条完全自洽，但结论是错的。**

**证伪实验**：只插 Type-C、**不开电池**，抓启动日志：
```
0x40  PCA9685 左半身        -> 应答 ✔   (491 us)
0x41  PCA9685 右半身        -> 应答 ✔   (273 us)
0x70  PCA9685 all-call 广播 -> 应答 ✔   (272 us)
写入的 SCL 超时也验证过：默认 8000，改成 80000 后失败耗时依然 1000 ms
```

**结论**：**假设错误。** 本机 PCA9685 的逻辑电源走 **USB 5V**，不走电池那路。

**附带收获**（意外之喜，两条）：
1. 定死了一块**电源拓扑** —— 这正是之前列在"待确认"里的项目
2. 意味着**所有 I2C / 逻辑层测试只插 Type-C 就安全**：不用开电池，舵机不可能动。这大大降低了后续调试的风险

**为什么会错**：把**参考设计**的电源树当成了**本机实测结果**。这就是 P-15 的另一个侧面。
**正确做法**：先测（1 次探测 ≈ 0.3 ms），再推理。

---

### H-2 ~~「IMU 藏在 GPIO18/19 上，只是从没被扫过」~~ **已推翻**

**提出依据**：
- 参考电路图把 21/22 分给 MPU6050、18/19 分给 PCA9685
- 而代码把 21/22 当舵机主总线、18/19 只当"失败回退"
- 全项目代码**从未把 18/19 当 IMU 总线扫描过**（`PA_STABLIZE.py` / `PA_WALK.py` 都是 21/22 → 32/33）
- 推论：**如果实物按参考图接线，那 IMU 一直是好的，只是从来没被扫到** —— "硬件阻塞项"可能根本不存在

（这条推理其实相当漂亮，我一度很看好它。）

**证伪实验**：
1. 三条候选总线用 `i2c.scan()` 权威扫描：`21/22` 只有 `0x40/0x41/0x70`；`18/19` 空；`32/33` 空（开内部上拉也是空）
2. 全引脚 **9 个引脚 × 72 个有序组合**（双极性）扫描：只有 `SCL22/SDA21` 出现器件
3. 后来位操作扫描再次确认：**112 个地址，命中 3 个** —— 就是 `0x40, 0x41, 0x70`

**结论**：**假设错误。本机没有安装任何 IMU。** 经 **4 次独立验证**。

**附带收获**：
- 这次证伪过程逼出了 P-08（假阳性）和 P-09（单方向）两个真实 bug —— **一个错误假设促成了两个正确修复**，这条经验值得记住
- 同时确认了"实物接线与代码一致"（PCA9685 确实在 21/22），`PA_SERVO.py` 不用改引脚

**为什么值得保留**：它证明了**假设写清楚 + 设计可证伪的实验**有多高效。一条命令就永久解决了争议，之后不用再纠结。

---

## 6. 待办：已知但尚未修的问题

| 编号 | 问题 | 计划 |
|---|---|---|
| P-16 | `padog.do_connect_STA()` 无超时死循环 | C 版 P5（`comm/wifi.c`）必须加超时 + 降级 |
| — | IDF 驱动 I2C 错误路径有 1 秒惩罚 | 已在 P-12 绕过扫描路径；但**后续任何 I2C 错误处理都要按 1 秒设计** |
| — | `web_c.py` 用 `exec()` 执行 URL 查询串 | C 版必须改白名单 + 范围校验（迁移表已列） |
| — | 标定保存会重写 `config_s.py` 源码 | C 版改 NVS |
| — | 本机无 IMU | F1 = A：暂不加装，P4 再议 |
| — | **配置以裸字节结构体存 NVS**：`sizeof(app_config_t) = 360`，布局依赖编译器的对齐/填充规则 | 已用 `version` + `crc32` 兜底（布局一变 → CRC 不符 → 拒绝载入并回默认值，**不会静默错读**）。**规矩：今后任何改动 `app_config_t` 字段都必须 `APP_CFG_VERSION + 1`**，并在 `test_app_config.c` 里同步更新期望值 |
| — | **I2C 带宽是 P3 的瓶颈**：100 kHz 下一次 PCA9685 通道写约 **0.5 ms**，12 路全写 = 6 ms，占满 100 Hz 周期的 60% | P2 已用"只写变化的通道"把稳态降到 0 次/帧；TROT 运动时 12 路都会变，届时若抖动超标，**下一步是把 I2C 提到 400 kHz**（PCA9685 支持 1 MHz，总线上只有两片），可降到约 1.5 ms。改之前先测抖动 |
| — | `motion` 的周期统计有一个约 3.7 ms 的**启动瞬态**抖动 | 观察到的：平均 9990 µs、最长 10000 µs、**超期 0 次**，属任务创建阶段的一次性现象。不影响稳态，暂不处理；若将来实时性要求提高再查 |

---

## 7. 变更记录

| 日期 | 内容 |
|---|---|
| 2026-09-19 | 建立本文件；收录 P-01 ~ P-16 与 H-1、H-2 |
| 2026-09-19 | P1 完成，追加 **P-17 ~ P-20**（Python/C 取整语义、对照测试自身的漏洞、原代码无效计算、相对路径写文件）与 F、G 两节；方法论扩到 15 条 |
| 2026-09-19 | P2 完成，追加 **P-21**（PCA9685 `off=4096` 的 bit4 被 `& 0x0F` 抹掉，**真 bug，从 P0 就存在**）与 **P-22**（golden 生成器写了参考值没用到的输入列）；方法论扩到 17 条；新增"验证手段本身出错"的汇总提醒 |
| 2026-09-19 | P3 开始，追加 **P-23**（参考值跑在手搭命名空间里，漏掉 `padog.py` 的 59 项默认值注入表 ⇒ `shank_ik_bias_per_mm` 取到死代码 0.375 而真值是 0.25，**参考值与 C 版错在同一个地方**）与 **P-24**（搜索模式的 `^` 锚定造成漏报，差点改掉一个正确的默认值）；G 类扩到 5 条 |
